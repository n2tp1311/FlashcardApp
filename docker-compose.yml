services:
  app:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data   # persist SQLite db between restarts
    environment:
      - PORT=3000
      - SESSION_SECRET=${SESSION_SECRET:-change-me-in-production}
    restart: unless-stopped
