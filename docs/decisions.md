# Decision Log

## 2026-08-09 — Flashcard session summary screen

Finishing a flashcard session previously called `returnFromStudy()` directly from the Finish button (the last card's relabeled Next button) — straight back to the lesson, no feedback of any kind. Quiz mode already had this covered via its Results screen (score ring/percentage/grade), so scope was Flashcard mode only.

**New per-session state, not persisted server-side**: `state.studySessionLog` (`{cardId: "learning"|"hard"|"known"|"confident"}`, written in `markCard()` alongside the existing `studyKnownMap` write) and `state.studySessionNewSet` (snapshotted once at session start in `startStudy()`, from `!card.last_studied_at` on the freshly-fetched card list — before any grading in the session could change it). Both reset per session, not accumulated across sessions.

**Keyed by cardId, not appended as an array** — re-grading the same card via Prev/Next (already possible before this feature) overwrites its entry rather than double-counting it, so the summary always reflects each card's *final* status for the session, not every intermediate grading event.

**"New" is a session-start snapshot, deliberately not re-checked at summary time** — after grading, a card's `last_studied_at`/`fsrs_state` would no longer be null even server-side, so re-deriving "was this new" from current data at the end of the session would make every graded card look like a review. The whole point of a new-vs-review count is "what was true when the session began."

**Skipped-card note**: `state.studyCards.length - Object.keys(studySessionLog).length` — cards the user navigated past without pressing a grade button (the mark buttons aren't mandatory before Next/Prev, only gated behind having flipped the card at least once). Shown only when nonzero.

**Reused `diffBar()`** (the same horizontal-bar component already powering the Dashboard's Card Difficulty Breakdown) for the 4-status breakdown, colored via `var(--danger)`/`var(--warning)`/`var(--success)`/`var(--primary)` — the same CSS custom properties the grading buttons themselves use, so the bars map 1:1 to the buttons the user actually pressed, and (unlike the difficulty breakdown's existing hardcoded hex colors) correctly re-themes for dark mode.

**Early Exit (`btn-fc-back`) deliberately skips the summary**, still calling `returnFromStudy()` directly — bailing out mid-session isn't "completing" it, and showing a completion-flavored screen there would be misleading.

**Revised same day**: grading the last card required a separate manual "Finish" click even though every other card auto-advances — inconsistent, and easy to miss (the button is just a relabeled Next). The same `fcAdvanceTimer` in `markCard()` that advances to the next card now calls `showFlashcardSummary()` directly when there is no next card, so finishing behaves like every other card grade: instant, no extra click. The manual Finish button (`#btn-fc-next`'s last-card label) stays as the only path for a last card the user *skipped* (navigated to without grading — `markCard()`, and therefore the auto-advance timer, never ran for it).

Removing that pause to look the last card over before it flies off-screen meant finishing had become a one-way door with no chance to look back. Added a **Review** button on the summary screen (`showScreen("flashcard")` + `renderFlashcard()`, leaving `state.studyIndex` wherever the session ended) so Prev/Next browsing is still available after finishing — "Back to Lesson" leaves for good, "Review" doesn't. Initially wired its label to the existing `summary.review` i18n key (the "N Review" stat-chip label), but caught before shipping that this collides semantically — the chip's "Review" is a noun (a card *category*, ↔ New), the button's "Review" is a verb (an action, "go look again"), and Vietnamese needs different words for the two ("Ôn tập" vs "Xem lại"). Split into a separate `summary.reviewAction` key.

## 2026-08-07 — Long-press-to-copy vs. swipe-to-grade conflict; swipe hint i18n

Two mobile-only bugs reported together: (1) long-pressing a flashcard to select/copy text would sometimes accidentally grade the card as known/unlearned; (2) the swipe hint labels always showed Vietnamese ("Biết rồi"/"Học lại") regardless of the app's language setting.

**Bug 2 was a straight i18n bypass, not a detection bug.** The hint `<div>`s (`client/app.js`, `initSwipeGestures`) were created once at script load with hardcoded `innerHTML` strings that never called `t()` — confirmed by contrast with the grading buttons sitting right next to them, which correctly use `data-i18n`. Fixed by reusing the existing `study.knowIt`/`study.learning` keys (same wording as the buttons) and adding `data-i18n` attributes, so `applyI18n()` — already called document-wide on every language switch via `applyLanguage()` — updates them automatically too, with no separate update path needed.

**Bug 1's root cause**: the swipe-to-grade gesture had a pure distance threshold (`Math.abs(dx) >= 8` px to arm, `75px` to commit) with no way to distinguish it from a long-press. A long-press's selection-handle/loupe dragging produces the same kind of touch-coordinate movement once the OS shows its native selection UI, so it could cross both thresholds and fire `.click()` on the grading buttons. The tempting fix — disable `user-select`/`-webkit-touch-callout` on the card — was explicitly rejected: the user wants long-press-to-copy to keep working, not be suppressed. Instead, the `touchmove` handler now checks `window.getSelection().type === "Range"` and defers entirely whenever a native selection is active (un-arming an in-progress drag if a selection starts mid-gesture), rather than guessing via a timing heuristic. This is a genuinely selective fix: a fast deliberate swipe never triggers the browser's long-press recognizer, so `getSelection().type` stays `"None"` throughout and the normal swipe path is completely unaffected.

Verified: a normal swipe (no active selection) still transforms the card and grades it correctly; the identical touch-coordinate sequence with a simulated active `Range` selection leaves the card ungraded and the transform reset.

## 2026-08-07 — Memory Interval Distribution chart: rename + fix silent period-pill bypass

Prompted by a user question challenging whether the chart genuinely reflects FSRS (it does — audited the full data path, `fsrs_state` + `srs_due_at`/`fsrs_last_review_at`-derived interval, zero leftover `srs_step` references). Two changes came out of that audit, both user-directed:

**Renamed** "SRS Interval Distribution" → "Memory Interval Distribution" in both locales. Explicit user call: end users don't need or care which scheduling algorithm is running under the hood; naming it after the algorithm (whether "SRS" generically or "FSRS" specifically, which was also considered and rejected) adds jargon without adding clarity.

**Fixed a real bug found during the audit, unrelated to naming**: the chart lives inside the period-pill-gated "Study Charts" section, under a note claiming "Applies to the charts in this section only" — but `GET /api/stats/srs-distribution` took no window param at all, so it silently ignored whichever pill (7/30/60/90 days) was selected and always showed an all-time snapshot. Every sibling chart in that section already respected the pill; this one didn't. Added an optional `?days=` param, scoping to cards last reviewed within that window via `date(COALESCE(fsrs_last_review_at, updated_at),'unixepoch') >= date('now', '-N days')` — calendar-day-aligned from the start, applying the same lesson learned from the Study Time Min/Max bug earlier today (a raw-seconds cutoff can truncate a boundary day and corrupt the result). Reproduced and verified with 3 cards backdated to 1/20/50 days ago: 7d window → 1 card, 30d → 2, 90d/all-time → 3, exactly as expected.

## 2026-08-07 — Avg/Min/Max Study Time gets its own day-window selector

Reverses a previously deliberate, repeatedly-reaffirmed choice (`docs/decisions.md:203-213` and two `docs/features.md` passages) to keep the hero card's Study Time stats strictly all-time, on user request. The headline "Study Time" total (the highlighted, larger stat) **stays all-time** — only Avg/day, Min, and Max per study day are now windowable. `server/routes/stats.js`'s `/dashboard` route computes these from two separate queries now: `totalMsRow` (unconditional, always all-time) and `studyTimeStatsRow` (windowed via an optional `?days=` query param, clamped 7-90 like `/analytics`, defaulting to `null`/all-time when omitted — fully backward-compatible, existing callers see no change).

**Revised same day**: the window selector initially shipped as an inline pill row directly on the hero card, but that added a row of clutter to an otherwise compact summary — moved into the existing "Customize metrics" modal instead, as one more row (`#dash-studytime-window-bar`) alongside the per-metric Hidden/Show/Highlight rows, staged in `_studyTimeWindowDraft` and only committed on Save (consistent with how the rest of that modal already works — `_dashMetricDraft` follows the same pattern). The card itself now shows the active window as a label suffix instead — `_dashMetricLabel(m, studyTime)` appends `"(7)"`/`"(30)"`/etc. to Avg/day, Min, and Max when a window is active, plain when all-time. The suffix is read from the actual `studyTime.windowDays` in the rendered server response, not the pending client state, so the label can never claim a window the displayed numbers don't match. Since the hero card is a single shared component rendered on both Home and Dashboard (`#home-summary-grid`/`#dash-summary-grid`), the selected window is still a persisted, shared `state.studyTimeWindowDays` (`localStorage["fc-studytime-window"]`) — picking a window on one screen is reflected on the other. Saving the modal does a real refetch (`store.getDashboard(days)`) only if the window actually changed; otherwise it falls back to the existing re-render-only path (`_refreshDashHeroCards()`).

Verified against seeded attempts spanning 1/3/20/45/85/200 days ago with distinct durations: every window's avg/min/max matched hand-calculated expectations exactly (both via direct API calls and through the modal → Save → card-label flow), and the all-time total stayed constant (1,500,000ms) across every window, confirming the headline stat is correctly unaffected.

The existing "Study Charts" period-note copy (`dashboard.periodNote`, below the unrelated 7/30/60/90d bar further down the Dashboard) said "the stats above are all-time" — now false for Avg/Min/Max specifically, reworded to call out their own control.

**Bug found and fixed same day, via a sharp user question** ("how can min(7) < min(all_time)?" — mathematically that should be impossible, since a window is a subset of complete days and its minimum can't legitimately undercut the minimum over all of them). Root cause: the windowed query's cutoff was `created_at >= strftime('%s','now') - N*86400` — a raw instant, not aligned to calendar-day boundaries — while still grouping by calendar day (`GROUP BY date(created_at,'unixepoch')`). The oldest day straddling that cutoff only had its post-cutoff attempts summed, a truncated fraction of what the all-time query computes for that same day; if that artificial partial-day total happened to be smaller than any genuinely complete day, it became the window's bogus minimum. Reproduced precisely: two attempts on the same real study day, one just before the 7-day cutoff instant (500,000ms) and one just after (5,000ms) — the old query only summed the 5,000ms sliver, reporting `Min(7)=5000` against `Min(all-time)=60000`. Fixed by aligning the cutoff to full calendar days (`date(created_at,'unixepoch') >= date('now', '-N days')`), so a day is either wholly counted or wholly excluded — never partial. Re-verified the invariant (`Min(window) >= Min(all-time)` for every window) holds against the same reproduction case.

## 2026-08-07 — Language-aware, realism-optimized TTS voice selection

Built via the full Explore → Plan → Implement → verify workflow. `_pickVoice()` (`client/app.js`) was English-only regardless of the app's UI language or the actual card content being spoken — a Vietnamese-UI user, or anyone studying Vietnamese card content, always got an English voice reading it, which is the single biggest lever against "realistic" speech (a premium English voice reading Vietnamese text sounds far worse than a mediocre Vietnamese voice reading it correctly).

**Detection is content-based, not UI-language-based**, with UI language (`state.language`) only as a fallback: `_detectSpeechLang(text)` scans the actual text for Vietnamese-exclusive diacritics before falling back. This serves the real scenario of a Vietnamese-UI user studying English vocabulary (or vice versa) — the spoken voice should match the text, not the menu language.

**The Vietnamese-detection regex is deliberately narrow** (`đ`/`Đ`, `ơ`/`ư` horn vowels, `ă`, and the Unicode `U+1EA0–U+1EF9` stacked-tone range) — it excludes base accented vowels shared with French/Spanish/German/Portuguese (à/á/â/è/é/ê/etc.), which alone would false-positive on card content in those languages (verified against French/Spanish/German test strings). Accepted trade-off: plain Vietnamese text using only shared-vowel accents and no tier-1 character (e.g. "Tôi tên là Phê") isn't caught and falls back to `state.language` — broadening the regex to catch it would reintroduce the French/German false-positive risk.

**Quality-tier ranking was broadened** from `enhanced|premium|neural` to also catch `wavenet|studio` (Android/Google Cloud-style naming) and `natural|online` (Microsoft Edge's neural voices, named like "...Online (Natural)") — applied uniformly to whichever language pool is active, not hardcoded to English.

**Caching moved from a single cached voice to a per-language cache** (`_ttsVoices` + `_ttsVoiceCache`), since the "right" voice can now differ per utterance based on content, not just be picked once at load — still refreshed together via the same `voiceschanged` event and Safari-synchronous-init workaround as before.

**Fallback behavior**: no matching voice for the detected language falls back to English (some voice is better than none) before finally returning `null` if literally zero voices exist at all — matches the existing silent-degradation convention (no `console.warn`/toast anywhere in this code path).

**No new preference, no schema/server change** — confirmed scope with the user before starting: this improves the existing automatic behavior, it doesn't add a voice picker UI or a cloud TTS integration (which would need an API key, incur cost, and require a backend proxy — a much larger change, considered and explicitly declined).

**Verification note**: voice *audio* quality can't be judged by an agent. Verified mechanically instead — a 9-case test-string matrix for `_detectSpeechLang` (pure English, Vietnamese tier-1, the existing `tts.testPhrase` i18n string, tier-2-only Vietnamese fallback under both UI languages, and French/Spanish/German non-false-positive checks) run directly against the shipped regex, plus `_pickVoice()` ranking/fallback assertions via Playwright with `speechSynthesis.getVoices()` stubbed to a fixed fake voice list. Attempting to also verify the real `speak()` pipeline end-to-end hit a Chromium native-binding wall (`SpeechSynthesisUtterance.voice` rejects plain fake objects, requiring a real internal `SpeechSynthesisVoice` instance) — a test-harness limitation, not an app-logic risk, since `u.voice = <voice>` is unchanged mechanics from the original code.

## 2026-08-06 — Replace fixed-step SRS ladder with FSRS

Built via the full Explore → Plan → Implement → verify workflow, informed by a Sonnet-research → Opus-review pass on spaced-repetition literature. Replaces `server/routes/attempts.js`'s flat, shared-for-every-card interval ladder (`SRS_INTERVALS`, 10min→1h→4h→1d→3d→7d→21d then doubling) with FSRS (`ts-fsrs`), a per-card Stability/Difficulty/Retrievability model. `card_states` gained `fsrs_stability`, `fsrs_difficulty`, `fsrs_state`, `fsrs_reps`, `fsrs_lapses`, `fsrs_learning_steps`, `fsrs_last_review_at`, `last_correct_source`; `srs_step` is left in place but inert (nothing writes or reads it anymore) rather than dropped — the `data/` directory shows a real DB corruption/recovery incident on 2026-07-24, so schema mutations on this table are treated with extra caution. Scheduler capped at a 365-day max interval (FSRS defaults to 100 years) to match the old ladder's behavior and avoid surprising this app's small user base with wildly long intervals.

**Rating mapping** (`server/fsrs.js`): `correct=false` → Again; `grade="easy"` → Easy; `grade="hard"` → Hard; `grade="medium"` → Good (dead code path, unsent by any client); ungraded `source="quiz"` → Hard; ungraded `source∈{flashcard,recall}` → Good. The quiz→Hard mapping is the direct replacement for the old `RECOGNITION_CAP_STEP` clamp — FSRS's Hard rating always yields a shorter next interval than Good for the same card by construction, so quiz recognition earns less credit than active recall without any clamp arithmetic. Verified live: quiz-no-grade and grade="hard" produce byte-identical FSRS output on equivalent cards, as do flashcard-no-grade and grade="medium".

**Migration for existing progress**: 547 `card_states` rows existed in production (276 with real SRS progress, `srs_due_at` set). A one-time backfill (`runMigration("fsrs_backfill_from_srs_step", ...)`, `server/db.js`) estimates each card's initial `fsrs_stability` from its old `srs_step` via the frozen old interval table (e.g. step 6 → 21 days), with a neutral default difficulty (5.0, no per-card difficulty signal exists in the old data to do better) — chosen over resetting everyone to FSRS defaults, so existing users don't lose SRS progress on upgrade day. `srs_due_at` is deliberately left untouched, so nobody's queue reshuffles. `last_correct_source` is backfilled from genuine `attempts` history (not invented), powering the Needs Recall redesign below.

**A real bug was caught and fixed during implementation, not after**: the first version of the backfill migration was placed *before* `server/db.js`'s `db.prepare` compatibility shim (which array-wraps multi-param `.run()` calls for `node-sqlite3-wasm`). Run once against production, it silently no-opped on every `fsrs_stability`/`fsrs_difficulty` UPDATE (no exception thrown, so it was marked "done" in `schema_migrations` despite doing nothing) — only the separate `last_correct_source` raw-SQL update worked, since it needed no parameter binding. Caught immediately via a pre-migration backup diff (routine safety practice, not luck), restored from that backup, moved the migration after the shim, and re-verified via a full isolated dry-run (copy of the real DB in a separate directory, never touching production) before re-applying: additive-only, idempotent on re-run, row counts and `srs_due_at` unchanged, `fsrs_stability` values matching the known `srs_step` distribution exactly.

**Needs Recall filter redesign**: the old check (`srs_step >= RECOGNITION_CAP_STEP`) has no FSRS equivalent — continuous stability has no step ceiling to compare against. Redefined around `last_correct_source === "quiz"`: a card "needs recall" if its most recent correct answer came from quiz recognition, never from actively recalling it in Flashcard/Recall mode — the same underlying intent, expressed directly instead of inferred from ladder position.

**SRS distribution chart**: rebucketed from `GROUP BY srs_step` (7 discrete integers) to FSRS state (Learning/Relearning get their own bucket) plus the same perceptual interval-range boundaries the old chart used (10m/1h/4h/.../1yr), so the chart's shape stays familiar despite the underlying scheduler now being continuous.

**Interval preview**: the flashcard screen's 4 grading buttons preview "what happens if you pick this" — previously recomputed step arithmetic client-side, now precomputed server-side (`server/fsrs.js:previewIntervals`, attached to card-list responses as `fsrs_preview_again/hard/good/easy`) so the preview can never drift from the authoritative scheduler. Rejected duplicating FSRS math in client-side vanilla JS (this app ships no bundler, and the parameter vector/fuzzing/retention-target config can change) and rejected a new dedicated preview endpoint in favor of piggybacking on the card-list endpoints the client already calls once per study-session load.

**Export/import bug fixed as part of this work**: `exportImport.js`'s import handler had explicit `INSERT` column lists that silently dropped `srs_step`/`srs_due_at`/`last_seen_at` (`card_states`) and `duration_ms`/`grade` (`attempts`) — export used `SELECT *` so the data was captured, but re-importing it reset every card's SRS progress to defaults. Pre-existing and independent of FSRS, but fixed here since it touches the same rows. Verified via byte-identical export → import → re-export round-trip.

## 2026-08-06 — Interleaved order guarantees round-robin alternation

The "Interleaved" study-order pill and "Shuffle" previously routed through the same code (`weightedShuffle` over the full flat card list), so interleaving across lessons was only true in expectation — random chance could still produce several consecutive same-lesson cards. Split them: "Shuffle" is unchanged (still a single global weighted shuffle, no lesson-awareness). "Interleaved" now groups filtered cards by `lesson_id`, weighted-shuffles each group individually (preserving the existing hard/medium/easy weighting *within* a lesson), then merges via a new `roundRobinMerge()` helper that takes one card from each group in turn. This guarantees strict alternation while every group still has cards; once a smaller lesson group is exhausted, the remaining groups' cards continue in their own order — expected round-robin behavior, not a bug. Verified deterministically (not probabilistically) with 3-card + 6-card lesson groups: `roundRobinMerge` produced the identical run-length pattern (`A,B,A,B,A,B,B,B,B`) across 3 repeated trials.

## 2026-08-06 — "Hard" flashcard grade: drop-by-1, not reset-to-0; grade persisted on attempts

A Sonnet-research → Opus-review pass over spaced-repetition literature found that `grade === "hard"` in `server/routes/attempts.js` reset `srs_step` to 0 — identical to an incorrect answer — but that branch was unreachable dead code, since no client UI had ever sent `grade: "hard"` (only "easy" existed, via the "Confident" button). Per Bjork & Bjork's desirable-difficulties research, a card recalled correctly but with effort should count as a successful review, just a weaker one — not a lapse. Added a 4th grading button, "Hard" (key `2`, positioned between Still Learning and Know It, matching Anki's Again/Hard/Good/Easy convention), that sends `grade: "hard"` and now computes `newStep = Math.max(curStep - 1, 0)` instead of resetting to 0. Deliberately reused the word "Hard" despite an unrelated pre-existing Easy/Medium/Hard difficulty-tier badge system (computed from historical accuracy) — the tooltip text ("recalled it, but it took some effort") disambiguates the two on hover. Keyboard shortcuts 1–4 were renumbered to match the new left-to-right button order (a one-time muscle-memory break for existing users, judged better than a permanent visual/key mismatch for all future users). Separately, `grade` is now persisted on the `attempts` table (`ALTER TABLE attempts ADD COLUMN grade TEXT`) — it arrived in every request already but was discarded after computing the SRS step, so no grade-based analytics or future FSRS-style scheduling work was possible.

## 2026-08-05 — New-card-today estimate: separate from "Max reviews per day," Setup screen not Dashboard

Built via the full Explore → Plan → Implement → verify workflow. The schema has no "new card" flag at all, and turned out to have two different, non-interchangeable existing notions of "new": `computeStats([]).level === "new"` (zero rows in `attempts`, used by the Dashboard's difficulty breakdown) vs. a card with no `card_states` row at all (never seen *or* graded — invisible to every SRS query in the app). Went with the second definition for "available new cards" (`LEFT JOIN card_states ... WHERE cs.card_id IS NULL`) since it's the one that actually matches "never introduced to this user," not just "never graded."

**Deliberately kept separate from the existing "Max reviews per day" preference**, even though both are "how much should I study today" concepts. That feature caps due-*review* load (`applyReviewCap`, `GET /api/stats/reviews-today`); this one estimates new-card *intake*. Different endpoint, different UI copy, no shared state — merging them would conflate "cards I already know that are due again" with "cards I've never seen," which behave completely differently for both the user's mental model and the SRS math.

**Algorithm**: average new-cards-introduced per day, computed only over days that had ≥1 introduction (mirrors the study-time hero card's "exclude untracked days, don't count as zero" convention), adjusted by a 7-day accuracy multiplier (1.15/1.0/0.7/0.4 by accuracy band, neutral at 1.0 if fewer than 10 recent attempts to avoid a near-empty week whipsawing the number), clamped to the real count of un-introduced cards remaining. Verified live across every branch before shipping: zero cards ever created, cards-but-no-history (cold start default), personalized with strong accuracy, personalized with degraded accuracy (multiplier correctly pulled the number down), and "everything already introduced" (correctly overrides a nonzero formula result to 0, with distinct copy from the "0 due to low accuracy" case so the two don't read as the same situation).

**UI placement: Study Setup, not the Dashboard.** This is a same-session, actionable number ("how many new cards should I add right now"), not a lifetime reflection stat — Setup is where the user is actively deciding what to study, matching the existing hint-line/live-match-count pattern already on that screen. Adding it to the Dashboard would've put a third "daily budget"-shaped number (after streak and reviews-remaining) on a screen where it can't actually be acted on.

## 2026-08-05 — Study Charts: 2-column masonry needed an empty-state fix, not just a column-count change

Removing the Study Time card (moved to the summary hero, see the hero-card entries above) left 4 chart cards in the "Study Charts" section's CSS multi-column masonry, and 3 columns left one column (SRS Distribution alone) with a large empty gap below it. Tried 2 columns instead — real screenshots at seeded data showed column heights within ~80px of each other, down from ~350px, a clear win — and set `columns: 2 320px`.

**But CSS multi-column "masonry" isn't true masonry — it can't freely place items wherever there's room.** It only chooses *where to break* a fixed DOM-order sequence into N groups; with 4 discrete `break-inside: avoid` cards and 2 columns, there are only 3 possible break points to choose from. That's fine when all 4 cards' heights vary continuously with real data (verified: the browser reliably found a good split). It broke down for a brand-new account: SRS Distribution and Upcoming Reviews both already collapsed to a one-line "no data" empty state when empty, but Heatmap and Weekly Trend didn't — they always rendered their full grid/all-zero-bars regardless of data — so the best available 2-group split was still ~549px vs. ~230px, reintroducing the exact class of problem the column-count change was meant to fix. Caught by deliberately testing a zero-data account, not just the populated seeded case.

**Fix was upstream of the layout, not another layout tweak.** Gave Heatmap and Weekly Trend the same empty-state collapse Future Due/SRS already had (reusing the existing `dashboard.noStudyData` i18n string rather than adding a near-duplicate key) instead of trying to out-clever the masonry algorithm further. Re-verified: empty-account column heights dropped from 549px to ~140px each, and it's a better first impression for new accounts regardless of the layout question. General lesson for this masonry pattern: it only balances well when there are enough small items for the browser's break-point search to be fine-grained, or when all items degrade toward zero height together — worth remembering if more chart cards are added or removed here again.

## 2026-08-04 — Lessons-list N+1 fix: `CROSS JOIN` to pin join order, not just split the query

A Railway-bot-authored PR (#1) correctly diagnosed `GET /api/classes/:classId/lessons`'s single query (`lessons LEFT JOIN cards LEFT JOIN attempts ... GROUP BY l.id`) as forcing SQLite to fan every card out across all its attempts before grouping, and proposed splitting it into three smaller queries merged in JS. Reviewed by pulling the branch into an isolated worktree and testing against seeded data rather than just reading the diff — the split itself was correct (verified byte-identical results against the old query, including empty-class and zero-attempts edge cases), but benchmarking at realistic scale (~8K cards, ~484K attempts across a user's account) showed the PR's `interactedRows` query could be *slower* than the bug it replaced: 301ms vs. 174ms for the original.

**Root cause, found via `EXPLAIN QUERY PLAN`:** the PR's query (`FROM cards c JOIN attempts a ON a.card_id = c.id WHERE c.lesson_id IN (...) AND a.user_id = ?`) let SQLite's planner choose to drive from `attempts.user_id` — scanning every attempt the user has *ever* made, across every class, then probing `cards` by primary key per row — instead of driving from `cards.lesson_id` (already scoped to this class) and seeking into `attempts` via the covering `idx_attempts_cu_created (card_id, user_id, created_at)` index the way the original query did. This regression hits hardest for exactly the long-time users with large attempt histories most likely to have hit the original bug's p99 tail.

**Fix: `CROSS JOIN` instead of plain `JOIN`.** SQLite treats `CROSS JOIN` as an instruction to respect the written table order and never reorders it, unlike a plain `JOIN` where the optimizer is free to pick either side as the driving table. Confirmed via `EXPLAIN QUERY PLAN` that this restores the `idx_cards_lesson` → `idx_attempts_cu_created` (covering) plan, and re-benchmarked at 49ms — faster than both the original bug and the as-submitted PR. Rewriting `a.user_id = ?` into the `WHERE` clause instead of the `JOIN ... ON` clause was tried first and did *not* change the plan; only `CROSS JOIN` did. Worth remembering if this query is ever touched again — it's easy to "simplify" `CROSS JOIN` back to `JOIN` without realizing it's load-bearing, since both are semantically identical and only differ in planner behavior.

**PR #1 was closed in favor of a direct fix**, referencing the PR in the commit — its own diagnosis and the three-query split were correct and did the harder diagnostic work; the query-planner fix was the one gap found during review.

## 2026-07-27 — Optional "type before flip" toggle, deliberately not a Recall-mode revival

User asked, mid-task: "while doing flashcard, I wanna to write out then compare with back flashcard to confirm whether know it or not, but make it optional only." Worth recording why this wasn't treated as a third attempt at Recall mode: this app had a full Recall mode (free-text answer + 3-button self-grade, a separate selectable Study Setup mode) removed 2026-07-02 for adding friction that interrupted study flow, and a later proposal to bring it back was independently rejected again for the same reason ("typing felt slow, self-grading felt subjective" — see the 2026-07-22 "SRS recognition-vs-recall gate" entry below). Flagged this history to the user before building anything.

**What makes this different, not just a rename:** it's an opt-in Preferences toggle (off by default, invisible unless turned on) living *inside* existing Flashcard mode, not a separate mode to choose in Study Setup. It never writes to `attempts`, never touches SRS scheduling, and doesn't add a grading pathway — the existing 3 mark buttons remain the only thing that grades a card, completely unchanged. The typed text is a pure self-comparison scratchpad: shown back at the user post-flip, then discarded (not persisted anywhere). Optionality directly addresses the exact objection the old feature couldn't survive.

**No auto-comparison/fuzzy-matching.** The typed guess and the real answer are just shown next to each other; nothing string-compares them or grades correctness automatically. The request explicitly framed this as self-judgment ("confirm whether know it or not"), and auto-grading a free-text answer would add false precision (a typo or a valid-but-differently-worded answer would misfire) — better left to the human doing the comparing, matching the framing of the request.

**Live verification (not just code review) caught a real bug**: pressing Enter in the guess input double-fired the flip — flip, then instant unflip on the same keystroke — because disabling the input (part of the flip, since there's nothing left to compare once flipped) forced a mid-keydown blur, and the same keydown event then bubbled to the document-level global Enter-to-flip shortcut, which no longer saw an input focused and fired again. Worse, it left the mark buttons wrongly enabled while the card was still front-facing — defeating the flip-gating safeguard added earlier this session specifically to prevent grading before seeing the answer. Fixed with `e.stopPropagation()` on the input's own keydown handler; re-verified live afterward (not just re-reading the diff) that the double-flip is gone, the empty-guess case still works, and the click-to-flip path wasn't broken by the fix.

## 2026-07-25 — Tags moved from lessons to classes/courses

User feedback right after the lesson-tags feature shipped: "course tag please, lesson is not needed as much." Explicit choice on scope: replace, not add both — removed lesson tags entirely (input, chips, filter bar, search matching, DB column reference) rather than keep both levels, matching CLAUDE.md's "if you're certain something is unused, delete it completely" rather than leaving a half-used feature around.

**Migration line removed, not reverted with a DROP COLUMN.** The `lessons.tags` column itself isn't dropped from already-migrated databases — given the DB corruption incident earlier this session, no schema-mutation risk was worth it for a column with no real user data in it. An unused leftover column is harmless: nothing reads `lesson.tags` anymore (confirmed via repo-wide grep after the removal), so any stale column on an already-migrated DB is inert JSON-string noise in a response, not a functional bug.

**Applied the two write-path lessons learned from the lesson-tags build's own code review, this time from day one.** Export/import round-trip and class-share cloning both got their `tags` column added to the classes INSERT immediately, rather than being discovered as data-loss gaps by a second review pass — verified with real end-to-end flows (delete + re-import; two-account share + clone), not just read-through. Code review this round still caught one more gap in the same family: `GET /shared-with-me` also needed the same tag-parsing treatment the public share-view endpoint got, since it's a third place classes get returned to a client. Completeness in this category seems to require deliberately grepping for every `SELECT * FROM <table>` / `SELECT t.*` on the table gaining a new JSON column, not just tracing the feature's own UI paths — noting this pattern explicitly in case a future column addition hits the same shape of bug.

**Chip placement changed proactively, not reactively.** Lesson tags had gone between the title and the meta line, sitting right next to a row that had *just* been fixed for title-crowding — a risk noticed only after the fact last time. Class tags went after the meta line from the start on both class-card layouts (Grid and List), designed around the known failure mode rather than rediscovering it.

## 2026-07-24 — Lesson tags: JSON column over a relational table, filter over a subdeck hierarchy

User pasted the "flat organization, no tags/nested folders" audit finding — bundles nested subdecks (the biggest lift, a real hierarchy data model touching navigation everywhere), tags + filtering, and cross-lesson custom study (already substantially covered by existing multi-class/multi-lesson selection plus the saveable Study Setup presets built earlier this session). Flagged the spread, user picked tags + filter, explicitly not subdecks.

**JSON column on `lessons`, not a relational `tags`/`lesson_tags` table.** Matches the existing lightweight-JSON-blob-column convention already used for `cards.data`, avoids a join for what's ultimately a small, per-user, per-class dataset filtered entirely client-side (mirrors how the existing Level/Format slicers already work — no server-side filtering infrastructure needed at this scale). A proper relational table would buy consistent cross-lesson tag autocomplete and cheap indexed lookups, neither of which matters until a user has far more lessons than this app's realistic scale — deferred, not rejected outright.

**Tags render as a new line below the lesson title, not inline in the row.** The lesson row was *just* fixed for title truncation earlier this session (reclaimed space from the format badge because the row was already tight) — cramming tag chips into that same horizontal space would have undone that fix immediately. Chips go on their own line below the title instead, where they can wrap freely without competing for the row's width budget.

**Single-select tag filter, not multi-select.** Every other filter/slicer in this app (format, level, class-level) is single-pill-active; a multi-select tag picker would be new interaction machinery not justified by this scope. Composes with the existing format filter via AND, matching how this app's other filters already stack (e.g. Home's Level slicer + Archived toggle).

**Code review caught two real, independently-confirmed data-loss bugs**: the `tags` column addition made two *pre-existing, untouched* lesson-creating INSERT statements stale — export→import round-trip (`server/routes/exportImport.js`) and class-share cloning (`server/routes/share.js`) both silently dropped tags, since neither statement's column list was updated when `tags` was added. Both are the kind of gap that's invisible until you specifically trace every write path for a new column, not just the ones the feature's own UI exercises — fixed by adding `tags` to both INSERTs, verified via a real export→delete→import round-trip and a real two-account share→clone flow (not just reading the diff). Also fixed a public share-view endpoint returning raw JSON-string tags instead of a parsed array (currently harmless, nothing reads it, but inconsistent with every other lesson-returning endpoint) and a client/local-mode normalization gap (the UI's tag parser had no 10-tag cap unlike the server, and `LocalStorageAdapter` had no normalization pass of its own) — unified both into one shared `normalizeTagsArray()` mirroring the server's `normalizeTags()` exactly.

## 2026-07-24 — Mobile truncation fixes: reclaim from the format badge, not the action icons

Two mobile findings from the audit's "Trải nghiệm Mobile" theme. Lesson titles in Class Detail were ellipsizing mid-word even at realistic lengths — measured live at 375px (an isolated worktree, not the real DB) rather than guessed: the title box only got 123px of a 351px row. The obvious lever (shrink/hide `.lesson-actions`, the edit/delete icons) was rejected — those were made deliberately always-visible on mobile in an earlier fix (M13) specifically because hover-gated icons are unreachable on touch; undoing that would trade one mobile bug for a worse one. Reclaimed from `.format-badge` instead (hidden on mobile) — it's the least essential piece of inline info (term-def/mcq/etc.), and still visible immediately once a lesson is opened, so nothing is permanently lost. Re-measured after the fix: the reported title now fits fully; a genuinely 50-character title still truncates, which is an acceptable, realistic limit at that screen width, not a regression.

Sidebar class names used a `title` attribute for the tooltip, which never fires on touch (no `:hover`) — a truncated name had no way to be read at all on mobile. Considered a JS tap-driven tooltip/toast; the codebase has no existing toast pattern to reuse, and a press-to-reveal interaction has a real usability conflict with the item's own tap-to-navigate behavior (releasing to read the tooltip would also trigger navigation). Instead switched to wrapping — `.sidebar-class-item` already used `min-height` rather than a fixed `height`, so the row grows cleanly to 2 lines with zero new interaction machinery.

## 2026-07-24 — Local dev DB corrupted by a bad restart pattern; recovered

A verification pass for the review-cap feature (below) corrupted `data/flashcards.db`. Root cause: the then-documented restart command in `.claude/skills/verify/SKILL.md` was `pkill -9 -f "node .*/server/index.js"` — that pattern requires a `/` immediately before `server/index.js`, which never matches a server launched with a relative path (`node server/index.js`, no leading directory) — exactly the launch command the same skill file recommends. The kill silently no-op'd, a second server process started and briefly held the same on-disk WASM file the first still had open, and the file corrupted (`PRAGMA integrity_check` failures).

**Recovery, not just a fresh-DB reset — this held real data** (`n2tp1311@gmail.com`'s actual classes: "Elements of Statistical Learning" 18 lessons/1,874 cards, "Forecasting" 7 lessons/1,108 cards, among others). Ran SQLite's `.recover` command, which rebuilt most rows into their correct tables but dumped 3,235 unattributed rows into a generic `lost_and_found(rootpgno, pgno, nfield, id, c0..c5)` table. Traced `lost_and_found` groups back to their real table by matching `nfield` (column count) and data shape against known schemas — the largest group (577 rows, `nfield=6`, uniform `c2='term-def'`) matched `cards`' exact 6-column shape; verified zero ID collisions with cards already recovered directly and 100% valid `lesson_id` foreign keys before merging them back into `cards`. A `nfield=2` group (2,654 rows, no `id`) was structurally an index, not row data — discarded, since indexes rebuild from table data automatically. A small `nfield=3` group turned out to be `sessions` rows for this session's own disposable test accounts — discarded as noise, not real user data. After merging, deleted the remainder of genuinely-unrecoverable orphan rows (rows whose parent record was truly gone, not just sitting in `lost_and_found`) and confirmed zero dangling foreign keys. Verified the recovered DB through the actual running app (not just raw SQL) — `/api/classes`, `/api/classes/:id/lessons`, `/api/stats/dashboard` all returned correct, complete data for the real account before promoting the recovered file back to `data/flashcards.db`.

**Fixed the root cause, not just the symptom**: `.claude/skills/verify/SKILL.md` now kills by port (`lsof -tiTCP:$PORT | xargs -r kill -9`, launch-command-agnostic) instead of the broken process-pattern match, and recommends an isolated `git worktree` (its own throwaway `data/flashcards.db`) for any verification that writes real data via raw SQL, so a future restart mistake can't touch the real database at all.

## 2026-07-24 — Max reviews per day cap: sessions capped, badges stay honest

User pasted the "no SRS algorithm customization" audit finding — bundles learning steps, max reviews/day, ease factors, graduating interval, and FSRS. Ease factors don't apply (this app uses fixed SRS steps, not SM-2); FSRS was already evaluated and deferred in an earlier decision. User picked the smallest contained piece: a daily cap on how many due cards get pulled into a review session (Anki-style).

**Design mirrors Anki's actual behavior**: the cap only limits how many due cards get queued into a session — it never changes what's "due" in the DB or what any badge displays. Dashboard/per-class/per-lesson due-counts stay true and uncapped; only the two places that actually start a review session (Study Setup's Due Only/Needs Recall filters, and the lesson screen's "Review N due" quick-start) respect the budget. Verified this invariant explicitly via Playwright: with the cap fully exhausted, the Dashboard's "Due for Review" section and the lesson's own due-count badge still showed the true count (10), while both session-start paths correctly yielded 0/an alert.

**"Reviews done today" approximates to any graded attempt today** (all modes/sources) — `attempts` has no new-vs-review distinction (no `srs_step`-at-attempt-time snapshot), so a true Anki-style "reviews only, not new cards" count isn't available without a schema change. Documented as an honest simplification, not hidden.

**Capped cards are chosen most-overdue-first**, not arbitrary DB order — `applyReviewCap()` sorts by `srs_due_at` ascending before slicing to the remaining budget, matching Anki's own prioritization. Verified via direct evaluation: `applyReviewCap([{due:1000},{due:2000},{due:500}], cap=2)` correctly returns the two earliest-due cards, not the first two in array order.

**Code review caught a real falsy-zero bug, fixed**: `parseInt(input.value, 10) || null` (save handler) and `!state.maxReviewsPerDay` (the cap-check itself) both treated a user-entered cap of exactly `0` as "no limit" instead of "study zero cards today" — `0` is falsy in JS. The `min="0"` HTML attribute implied 0 was a supported value, but there was no way to actually set it end-to-end. Fixed by switching every check from truthy to explicit `=== null`/`=== undefined`, and re-verified via direct state manipulation in an isolated worktree (see incident entry above) that `cap=0` now correctly yields zero cards, `cap=null` yields all cards, and `cap=2` yields exactly the two most-overdue.

**Not fixed, left as known low-severity gaps**: Study Setup's live match-count preview doesn't recompute if the user opens Preferences (reachable from the global header) and changes the cap without touching a filter pill — the actual session-start still applies the live value correctly, only the preview text can be briefly stale; fixing it would couple Preferences' save handler to Setup-screen-specific recompute logic for a narrow interleaving. Server-side validation of `maxReviewsPerDay`'s shape/range in `PUT /api/auth/preferences` was also skipped — matches existing precedent, since no other preference field (darkMode, fontScale, ttsRate, language) is validated server-side either.

## 2026-07-24 — Dashboard chart cards: CSS masonry instead of a uniform grid

User feedback: the 5 Study Charts cards should "scale to fit the frame, no empty space" — a uniform `display:grid` with `align-items:start` left cards floating at mismatched heights within a row (a 2-row SRS Distribution card next to a 12-row Weekly Trend card, neither stretching to match), and once Future Due became a 5th card, 5 cards in a 3-column grid left one entire cell blank in the last row. Considered stretching cards to equal height (`align-items:stretch`) — rejected, since a stretched-but-mostly-empty card (e.g. "Study Time," a single number) would just move the blank space inside a card border rather than remove it. Switched `.analytics-charts-grid` to CSS multi-column (`columns: 3 260px`, each card `break-inside: avoid`) — cards pack into whichever column has room next regardless of individual height, which is robust to future chart additions/removals without needing to hand-tune which cards pair together. Verified via Playwright screenshots at 1400px and 390px (mobile collapses to a single column, same as before).

## 2026-07-24 — Future Due forecast: one narrow slice of "shallow analytics," fixed window, one real bug caught in review

User pasted 4 more "So sánh đối thủ" findings (new study modes, deeper analytics, tags/subdecks, richer card types) — again flagged these as multi-week product-scope features rather than batch-implementing them, and asked the user to pick a slice. They chose the narrowest piece of the "shallow analytics" finding: a Future Due forecast (bar chart of cards becoming due over the next 14 days), leaving the rest of that finding (retention curve, per-card history, ease/button distribution, review-time stats) and the other 3 findings undecided.

**Fixed 14-day window, deliberately not tied to the Dashboard's existing 7/30/60/90-day period pills.** Those pills are backward-looking (past attempts); Future Due is forward-looking (upcoming due dates from `card_states.srs_due_at`). Reusing the same pill control for both would conflate two different axes of time. `windowDays` is returned by the server response rather than duplicated as a constant in both files.

**Query only reflects currently-scheduled due dates, not a simulation.** `WHERE srs_due_at > now AND srs_due_at <= now + 14d, GROUP BY date(srs_due_at,'unixepoch')` — doesn't project what happens after each of those cards gets reviewed and rescheduled again. Matches how Anki's own basic Future Due works, and keeps the query trivial (mirrors the existing `/srs-distribution` endpoint's exact shape, including its precedent of not filtering archived classes for this particular table).

**Code review caught a real bug before it shipped:** the client's day-bucket loop started at "tomorrow" (`i=1`), but the server's `srs_due_at > now` condition correctly includes cards due later *today*, grouped under today's UTC date. Since the client never queried that bucket, an account with cards due only later today would have shown the chart's empty state ("No cards due in the next 14 days") despite genuinely having due cards — a false negative on exactly the information this chart exists to surface. Two independent review angles caught this identically. Fixed by starting the loop at `i=0` with a new "Today" label (new `time.today` i18n key), rather than shifting/truncating the far end of the window to keep the row count unchanged.

**Not fixed, left as a known low-severity edge case:** `/future-due` and `/dashboard` each evaluate "now" independently (SQLite `strftime('%s','now')` vs JS `Date.now()`) at request time; since `renderDashboard()` fires both concurrently via `Promise.all`, a card due in the next few seconds could theoretically be double-counted or dropped across the two widgets in a millisecond-scale race window. This matches an existing pattern already present elsewhere in the app (independently-evaluated "now" across separately-fetched time-windowed endpoints) — not a new problem introduced by this feature, and self-corrects on the next load. Fixing it would mean threading a single shared timestamp across independent HTTP requests for a cosmetic edge case; not worth it.

## 2026-07-24 — Two items from the "So sánh đối thủ" (Anki/Quizlet) audit theme; the rest deferred

User pasted 8 competitor-comparison findings. Most (custom SRS algorithm parameters, Anki `.apkg` import/export, new study modes, deeper analytics, nested tags/subdecks, richer card types/media, offline-first) are multi-week product-scope features, not audit bugs — asked the user to scope down rather than batch-implementing them as if uniform in size. User chose two contained items: the Dashboard/Analytics nav duplication (a real bug) and saveable Study Setup presets (a genuinely small, additive feature). The rest remain undecided, to be scoped individually later.

**Dashboard/Analytics nav duplication root cause, confirmed via `git blame`:** commit `7da10af` ("modernize home layout with Quizlet-style sidebar") replaced the old inline-header-button + `⋮`-dropdown nav with a sidebar, deliberately leaving the old markup in the DOM but permanently hidden (`<!-- Keep for compatibility, hidden always -->`) rather than deleting it — a conscious decision, not a bug, and left untouched. What *was* a bug: that same commit's sidebar originally shipped both a `sidebar-dashboard-link` and a `sidebar-analytics-link`; the latter was deleted from `index.html` at some later point but `#btn-analytics`/`#btn-analytics-inline` and their click handlers (identical body to Dashboard's — there's no `#screen-analytics`, never was) were never cleaned up, becoming pure orphans reachable only via the `A` keyboard shortcut, whose keymap-modal label still said "Analytics". Fix: deleted the orphaned duplicate, relabeled the keymap row "Dashboard". Deliberately did *not* revive the old inline/⋮ nav mechanism — that's a separate, unrelated, larger change nobody asked for.

**Study Setup presets scoped to server mode only, matching every other preference in the app.** `loadUserPreferences()` (which now also caches `studyPresets`) is already gated `if (!IS_SERVER) return` for dark mode/font scale/TTS rate/language — presets follow the same rule rather than building parallel localStorage-only logic for local mode, which already can't correctly filter by `due`/`needsRecall` anyway (no `srs_step` tracking, per `LocalStorageAdapter`'s existing documented limitation).

**No preset-name uniqueness check.** Verified via Playwright: saving two presets with the same name is allowed silently, producing two visually-identical chips distinguishable only by click position. Left as-is — matches the app's existing minimal-validation posture (the `PUT /api/auth/preferences` endpoint doesn't validate any other preference field's shape either), and a real collision requires the user to type the same name twice on purpose. Revisit if it turns out to actually confuse people in practice.

**Code review caught two real (if minor) bugs, both fixed:** (1) the *existing* `btn-save-preferences` handler overwrote the local `fc-preferences` localStorage cache wholesale instead of merging, so saving regular Preferences after saving a preset would silently drop `studyPresets` from the local cache until the next server fetch re-synced it (server copy was never at risk — the PUT endpoint does its own read-modify-write merge). Fixed to merge, matching the pattern the new preset code itself established. (2) `applyStudyPreset()` was missing the needsRecall→flashcard mode guard that the manual filter-pill click handler enforces (Quiz mode can't structurally advance needsRecall cards further) — currently unreachable in practice since presets can only be captured from already-consistent UI state, but a latent gap if presets are ever edited or imported later. Fixed defensively.

## 2026-07-24 — Dropped the legacy "Recall" pill from Overall Accuracy (reverses an earlier call)

`renderAccuracyBySourcePills()` originally rendered a third "Recall" pill whenever an account had any `source='recall'` attempts left over from the removed Recall mode — a deliberate choice at the time to preserve visibility into old data rather than silently fold it into an aggregate. User feedback: Recall isn't a mode anymore, so it shouldn't visually read as one alongside Quiz/Flashcard. Changed `renderAccuracyBySourcePills()` to unconditionally iterate only `["quiz", "flashcard"]`, and removed the now-dead `ACCURACY_SOURCE_KEYS.recall` entry and its `dashboard.accuracyBySourceRecall` i18n strings (EN/VI). Legacy recall attempts still count toward the overall accuracy number and total (no DB/query change) — they just never get their own pill, verified via Playwright with both a mixed-source account and a recall-only account (empty pills row, not a fallback "Recall" pill).

## 2026-07-24 — "Ma sát & Tốc độ thao tác" batch: shared setup-data promise, flip-gate at the gesture layer, and a deliberate non-fix

Implemented the 4 remaining findings from the audit's Friction & Speed theme.

**Study Setup's live match-count reuses one prefetched promise (`state.setupDataPromise`) instead of fetching per pill-click.** `openSetup()` now kicks off `store.getBulkCards(ids)` once, immediately on screen-open; both the live count (recomputed on every `setup-filter` pill click) and `startStudy()` await the same promise. This is the same shared-promise-plus-monotonic-request-id pattern used earlier this session for the Stats-screen trend-fetch race — a stale `openSetup()` call (user re-enters Setup for a different scope before the first fetch resolves) is discarded by comparing `state.setupRequestId` at resolution time, rather than by cancelling the underlying fetch. The filter-selection logic itself was extracted into `filterCardsBySetup()` so the live preview and the actual Start action can never compute a different card set from each other. Code review caught that the original promise chain had no `.catch()` — a rejected fetch would leave "Start Studying" silently doing nothing forever; fixed by resolving to an empty card set on failure so the existing zero-card alert path fires naturally instead of adding new error UI.

**Flip-before-grade gating lives at the gesture layer, not just the button layer.** The three mark buttons get `.disabled` until `state.studyHasFlippedCard` is set, which also blocks the `1`/`2`/`3` keyboard shortcuts for free (a disabled button's `.click()` no-ops). Code review (two independent finder angles, same finding) caught that the swipe-to-grade gesture calls `.click()` on those same buttons programmatically but doesn't check the flag first — without a fix, swiping an unflipped card would play the full fly-off animation and then silently fail to grade, a worse UX than not gating swipe at all. Fixed by checking `state.studyHasFlippedCard` at the point the drag *starts* (in `touchmove`, before `fcDragging` is set), so an unflipped card doesn't animate at all rather than animating and then reverting.

**Investigated, not fixed: Dark Mode's new live-preview toggle doesn't revert if Preferences is closed without Save.** This was flagged as a plausible-but-not-new-behavior. Font scale and TTS rate already live-preview on click with the same non-revert behavior — there is no dedicated Cancel button anywhere in this modal that implements revert-on-close for any of the three settings, only a Save button. Adding revert semantics for just Dark Mode would make it *inconsistent* with the other two live-previewed settings rather than fixing anything; left as-is to match existing precedent. Would revisit only as part of a modal-wide "revert on close" feature covering all three settings at once.

## 2026-07-24 — "Dữ liệu & Độ chính xác" batch: not-due hint timing, and one non-fix

Implemented the 7 remaining findings from the audit's Data & Accuracy theme. Two decisions worth recording:

**Flashcard's not-due hint is computed client-side, synchronously — not from the server's new `notDue` response field.** Quiz mode's equivalent hint (`showQuizHint`, shared with the pre-existing recognition-cap hint) does wait on the network response, which works fine there because Quiz already auto-advances on a 1200ms timer — plenty of margin. Flashcard's auto-advance was a fixed 400ms, too tight for a round-trip to reliably land before the card changed. Rather than slow down every Flashcard grade waiting on the network, the same `card.srs_due_at` check already used to blank the interval-preview text is reused to decide synchronously, and the auto-advance delay is only extended to 1200ms in that specific case (normal grades stay snappy at 400ms). Code review then caught that the existing 400ms `setTimeout` was never captured or cancelled — a latent race (navigate away right after grading, old timer fires later and yanks you forward again) that the new 1200ms window made three times more likely to actually manifest. Fixed by mirroring the exact `state.quizAdvanceTimer` capture-and-cancel pattern Quiz mode already had for the identical problem.

**Investigated, not fixed: `renderLessonBreakdown()`'s accuracy also does `total > 0 ? ... : 0`, the same "0% vs no-data" shape fixed elsewhere this batch.** Traced it: the server query feeding that function already has `HAVING total_attempts > 0`, so a zero-attempt lesson never reaches the `: 0` branch in practice — the empty state is handled separately, one level up, by the `!rows.length` check. No code change; noted here so a future pass doesn't "fix" a branch that can't execute.

## 2026-07-23 — Study streak based on `attempts`, not `quiz_sessions`

User report: studied yesterday, streak still reset. `quiz_sessions` rows are only written when a Quiz session reaches the results screen (`saveQuizSession()`) — Flashcard-mode study, and any Quiz session started but not finished, never wrote one, so the streak calc (`SELECT DISTINCT date(taken_at,'unixepoch') ... FROM quiz_sessions`) couldn't see that day at all. Confirmed against a real test account: attempts existed for the previous day via Flashcard/Quiz answers, but zero `quiz_sessions` rows ever — streak was 0. Switched the streak's day-list source to `attempts.created_at`, which both modes write immediately (per Flashcard grade, per Quiz answer, not just on quiz completion) — after the fix, the same account correctly showed streak 1. The separate "Sessions" stat on the dashboard summary grid is intentionally left reading from `quiz_sessions` (it's meant to count completed quizzes specifically, a different metric from "did you study"); only the streak's data source changed.

## 2026-07-23 — Dropped edge-swipe-to-open-sidebar on Home (fights iOS's native back gesture)

The pull-to-refresh fix above turned out to be real but incomplete — the user (on an installed iOS PWA, Add to Home Screen) still saw a blank white flash on the same gesture. Root cause: a left-edge swipe-right is exactly the input WKWebView's own back-navigation gesture recognizer watches for, and this is active even in standalone/home-screen mode — a long-standing iOS limitation with no CSS/JS API exposed to web content to disable it. Our edge-swipe handler's touch listeners are registered `{ passive: true }` (required so `touchmove` doesn't block scrolling), which means they structurally cannot call `preventDefault()` to suppress the native gesture even in principle. Made worse by this session's earlier browser-Back-trap fix: it calls `history.pushState()` on load specifically so a native Back press has something to intercept — which also means WKWebView's native gesture now has real history to act on, where before it may have had none.

Considered: shrinking or shifting our own edge-detection zone away from the true edge. Rejected — iOS's recognizer arms based on where the physical touch lands relative to the screen edge, not on anything our JS decides, so narrowing our own threshold doesn't stop the OS from claiming the gesture first.

**Update, same day:** dropping our own handler didn't stop the flash either — confirming the native gesture fires independently of any page JS, not in response to ours. Tried the actual standard mitigation instead: a dedicated non-passive `touchmove` listener scoped to the leftmost 24px that calls `preventDefault()` once the drag is recognizably horizontal, before iOS's recognizer commits (a passive listener, as this handler originally was, cannot call `preventDefault` at all — that's a separate reason the first attempt couldn't have worked). Restored the swipe-to-open-sidebar behavior on Home under this suppression. This is a known-imperfect technique — reliability varies by iOS version and there's no way to verify it from this environment (Chromium's touch emulation doesn't run WKWebView's real gesture recognizer). If the flash still happens after this, the technique isn't landing on that device and the gesture should be dropped from Home for good, same conclusion as the entry above, just arrived at through the correct mitigation attempt first.

Resolution: dropped only the Home-screen case (swipe-right → open sidebar), since that's what was reported and Home has no "back" target to conceptually align with anyway. Left the "swipe right → go back" behavior on class/lesson/quiz/stats/dashboard screens as-is — same theoretical conflict exists there, but both gestures agree on *direction* (back), so any visual overlap reads as a normal transition rather than a jarring glitch, and it wasn't reported as broken.

## 2026-07-23 — Pull-to-refresh needed a horizontal-motion guard

User report: swiping right to open the sidebar sometimes showed a blank/white panel briefly. Root cause: pull-to-refresh (`client/app.js`, mobile-only) only ever checked the vertical delta (`dy`) against its 72px threshold — it had no check on the horizontal component at all. The edge-swipe-to-open-sidebar gesture only requires the vertical drift to stay *under* 60% of the horizontal drift (`Math.abs(dy) < dx * 0.6`) to count as a sidebar-open swipe, which leaves plenty of room for `dy` to still cross pull-to-refresh's 72px threshold on the same gesture — firing both handlers at once. Pull-to-refresh's refreshing state (`renderHome()` + a 1200ms spinner) has no background color set on `#ptr-indicator`, so it reads as a blank/white bar pushed in from the top, landing right on top of the sidebar's slide-in animation. Fixed by adding the reciprocal check pull-to-refresh was missing: bail out if `|dx| > dy` (gesture is more horizontal than vertical), mirroring the guard the edge-swipe handler already had. Verified via Playwright by dispatching synthetic `TouchEvent`s for both a realistic diagonal sidebar-open swipe (sidebar opens, refresh stays inert) and a pure vertical pull (refresh still fires) — Playwright's touch-event dispatch works fine here since these are just DOM listeners, not native OS gesture recognizers.

## 2026-07-23 — Analytics/Stats fix batch: what moved, what stayed all-time, and what wasn't a bug

Implemented all 9 findings from the UX audit's Analytics theme in one pass. Several sub-decisions worth recording:

**Struggling Lessons moved to the windowed `/analytics` endpoint; Overall Accuracy and Difficulty Breakdown did not.** The audit flagged that the period selector (7/30/60/90d) silently only affected 3 of 7 dashboard widgets. Rather than force every widget to respect the period, only Struggling Lessons moved — it's the one where staleness is actively misleading (a lesson can stay flagged long after the user fixed it), and it already sits at the bottom of the screen, separate from the accuracy/difficulty widgets that render *above* the period bar. Moving those two into the windowed section would have meant reordering existing screen markup for two widgets whose all-time framing is also a defensible product choice (a lifetime mastery score is a normal thing to show). The existing period-note ("stats above are all-time") still holds. Struggling Lessons also gained a minimum-sample-size guard (≥3 attempted cards) it never had — a lesson with 1-2 unlucky cards no longer gets flagged the same as a lesson with a real pattern. Trade-off: a small-but-genuinely-struggling lesson (1-2 cards, both hard) no longer surfaces on the dashboard at any window; the per-lesson Stats screen (Hardest Cards / Accuracy Trend) still shows those individual cards to anyone who drills in.

**Accuracy-by-source pills windowed, unlike the parent Overall Accuracy bar.** Slight inconsistency, chosen deliberately: the per-source split is a diagnostic ("how am I doing lately, and does one mode skew my numbers") where recency matters more than the lifetime headline figure sitting right above it.

**SRS Interval Distribution's "buckets are too coarse" finding was not a bug.** `stepLabel()` already covers the full 12-bucket range (10m through 1yr) — the audit's test account simply had no cards that had advanced past step 2 yet. No code change; documented here so it isn't rediscovered as a bug later.

**Duration clamped to 5 minutes server-side, not client-side.** A card can be "shown" for hours if a tab is backgrounded or the device sleeps mid-session; trusting the raw `Date.now()` delta would let one outlier attempt dominate the "Study Time" total. Clamping only at the `POST /api/attempts` write (not also in the CSV export or the `SUM(duration_ms)` query) follows the existing codebase convention of validating at the boundary and trusting internal code afterward — `duration_ms` has exactly one write path today.

**CSV export gained hour/class/mode/duration columns, not grade or session-id.** Grade is never sent for Quiz attempts (only Flashcard's optional "Confident" button sends one), so a grade column would be mostly empty and imply objectivity the data doesn't have. Session-id has no home on `attempts` at all — Quiz sessions get one but Flashcard attempts have no FK back to it — and adding one is schema work disproportionate to a CSV column.

## 2026-07-23 — Two mobile layout bugs found by screenshot; verified with Playwright's WebKit engine, not Chromium

User-reported screenshots on a real iPhone showed a class title collapsing to one character per line in List view, and the Flashcard grading-button row overflowing off-screen. Both traced to real bugs (documented in the commit), but the List-view one specifically only reproduced under WebKit — an isolated repro rendered correctly in Chromium at the same viewport width, and only WebKit's flexbox handling of the always-in-flow-but-invisible `.class-list-actions` (opacity:0, no `display:none`) actually squeezed the title to ~40px. This session's mobile verification had exclusively used `chromium.launch()`; installed and used `webkit.launch()` for this one, since Playwright ships an actual WebKit build. Worth reaching for WebKit specifically for future mobile CSS bugs that don't reproduce in Chromium, since real iOS users are on WebKit.

Also caught mid-fix: adding `.class-list-actions { display: none }` to the *existing* shared mobile media-query block (top of the file) had no effect, because the class's un-scoped base rule (`display: flex`) is declared later in the file — with equal specificity, source order decides, and the later rule won regardless of the media query matching. Fixed by placing the override in its own `@media` block immediately after the base rule instead. General lesson for this file: a mobile override only reliably wins if it appears after the rule it's overriding, not merely inside a `max-width` block — CLAUDE.md's "no line-item enumeration" aside, this is worth remembering next time a mobile-only rule doesn't seem to apply.

## 2026-07-23 — Fixed undefined `--bg2` CSS variable (invisible sidebar hover labels in dark mode)

User-reported screenshot: hovering "Dashboard" in the sidebar in dark mode showed a near-invisible label. Root cause: `--bg2` was referenced in 14 places (`.sidebar-nav-item:hover`, `.icon-btn:hover`, etc.) but never defined in either theme's `:root` block — the actual hover-surface token is `--surface2`. Three of the sidebar rules had a hardcoded `#f0f2f7` fallback that masked the bug in light mode; in dark mode the fallback produced a light background under `color: var(--text)` (near-white in dark mode), collapsing contrast to near zero. The other 9 usages (no fallback) silently rendered no hover background at all in both themes. Renamed every consumer to `--surface2` rather than defining `--bg2` as an alias, since `--surface2` was clearly the intended, already-themed token.

## 2026-07-23 — SRS interval preview suppressed (not zeroed) on not-yet-due cards

The Anki-veteran audit persona flagged that the grading buttons don't preview the next review interval, unlike Anki's "Again/Hard/Good/Easy" bar. Added `.mark-int` text on each of the three Flashcard buttons, computed from the card's `srs_step` via the same `stepLabel()` table the Dashboard's SRS-distribution chart already uses — no new interval table introduced. The code-review pass caught a real bug in the first version: the preview always assumed grading would advance the card, but `server/routes/attempts.js` has an early-return for not-yet-due cards (the default Study Setup filter is "All", so non-due cards do appear in Flashcard sessions) — grading one leaves `srs_step`/`srs_due_at` completely unchanged no matter what's clicked. Considered showing "no change" text for that case, but decided an empty label communicates it more plainly than another string to translate for an edge case. Also fixed a related staleness bug: `markCard()` was discarding the attempt response, so re-grading the same card via Prev showed a preview computed off the pre-attempt step; now the response's `srs_step`/`srs_due_at` are written back onto the in-session card object.

## 2026-07-23 — Deduplicated the Escape-key modal-close waterfall against the new browser-Back trap

Adding the popstate-based Back trap (see the browser-back-button entry in `docs/features.md`) required a "close whatever's open" helper (`fcCloseTopModal`/`fcAnyOverlayOpen`), which turned out to be an exact line-for-line duplicate of the pre-existing Escape-key handler's five-modal waterfall. Rather than ship two copies of the same modal-id list that could drift apart, the Escape branch now calls the new shared helper instead of re-implementing it. Verified via Playwright that Escape still closes the search modal exactly as before.

## 2026-07-23 — Flashcard nav two-row layout is now the default, not mobile-only

The earlier "mobile-only" fix for the 3rd grading button (see the 2026-07-22 manual-grading entry) was incomplete. `.fc-nav` is capped at `max-width: 600px` at *all* viewport widths, and Prev + three grading buttons + Next total ~700px — so the overflow the mobile fix addressed also happened on desktop: `justify-content: space-between` can't add negative space, so the buttons packed left and the Next button spilled past the nav's right edge, shifting the grading-button group ~50px right of the card/dots center (measured at 1440px and 1912px viewports). This is the "lệch layout" the user reported. Moved the wrap rules (`flex-wrap` on `.fc-nav`; `.fc-mark-btns { flex-basis: 100%; order: -1 }` + `.btn-mark { flex: 1 }`) out of the `max-width: 600px` media query and into the default block, so the grading buttons always take their own full-width row (three equal buttons matching the 600px card width) with Prev/Next below. Desktop and mobile now share one correct layout; the media query keeps only the small-screen padding/font-size reduction. Verified centered with no overflow at 390 / 1440 / 1912px.

## 2026-07-22 — Study Setup keyboard shortcuts (Enter to start, Esc/Backspace to go back)

The Study Setup screen was the one gap in an otherwise fully keyboard-drivable Home→Class→Lesson→study path — flagged independently by the impatient-user and keyboard-power-user audit personas, then by the user directly. Added a `screen === "setup"` branch to the global keydown handler: `Enter` clicks Start Studying (with `preventDefault` so a focused pill's native Enter-activation doesn't double-fire), `Esc`/`Backspace` go back. Chose not to add per-pill shortcuts (number keys for card-count/filter/mode/order) — four pill groups would need overlapping digits and the real friction was just "can't start without the mouse"; the S→Enter path now runs a full session hands-free. Added a matching "Study Setup" section to the `?` shortcuts modal, placed between Lesson and Flashcards to mirror the navigation flow.

## 2026-07-22 — Multi-agent UX audit: 10 personas, 8 completed; fixed the two ≥3-agent-confirmed bugs

Ran 10 parallel agents against a live instance, each logging into a separate seeded test account and exploring the app as a distinct demanding persona (impatient/speed-focused, confused new user, keyboard-only power user, visual design critic, mobile-only user, data-accuracy academic, Anki/Quizlet veteran, cautious/data-loss-averse, bilingual EN/VI tester, analytics-focused quantified-selfer). 8 completed; 2 (Anki-comparison, data-loss-averse) failed mid-run on the account's monthly spend limit — not retried, since retrying would hit the same wall immediately.

Rather than acting on the full ~50-item raw findings list, scoped this pass to only the two clusters independently confirmed by 3+ agents, on the reasoning that independent corroboration across differently-briefed personas is a much stronger signal than any single agent's opinion — the full list (severity-graded, per-agent) is preserved separately for manual prioritization rather than acted on wholesale.

**Fixed — "Last seen: never" / "Next review: not scheduled" despite real attempt history** (independently hit by the new-user, data-accuracy, and analytics personas, each via a different repro path). Root cause: `GET /api/stats/lesson/:id` — the endpoint `renderCards()` actually calls in server mode — ran a bare `SELECT * FROM cards`, never joining `card_states` or the attempts-derived `last_studied_at`, so `card.last_seen_at`/`last_studied_at`/`srs_due_at` were always `undefined` on the client regardless of real DB state (`relativeTime()`/`futureRelativeTime()` fall back to "never"/"not scheduled" on any falsy input). The sibling endpoint `GET /api/lessons/:lessonId/cards` already had the correct JOIN; copied that exact pattern rather than writing a new one. `GET /api/stats/class/:id` has the identical bug but is currently unused by any client code path — left alone as genuine dead code, not silently "fixed" without a caller to verify against.

**Fixed — "Easy" naming collision between the Flashcard grading button and the difficulty-tier badge** (flagged independently by the impatient, new-user, and design-critic personas, from three different angles: "badge says Easy after I clicked a *different* button," "I don't understand what Easy vs Know It actually does," and "the Easy badge/dots are green but the Easy button is blue — inconsistent color mapping"). These are two genuinely unrelated systems that happened to share a word: a difficulty *tier* (Easy/Medium/Hard/New, computed from historical accuracy, shown as badges/dots everywhere) and a grading *button* (a +2-SRS-step confidence rating, only in Flashcard mode). Renamed the button to "Confident" (`study.confident`/`study.confidentHint`, replacing `study.easy`/`study.easyHint`) rather than touching the difficulty-tier vocabulary, since that color/word system is used pervasively (Stats, Dashboard, card badges, the "Hard First" filter) and renaming it would have been a much wider-blast-radius change for the same fix. The button's internal `grade: "easy"` value sent to the server is unchanged — only the user-visible label moved.

## 2026-07-22 — Edit-in-study card lookup uses the in-memory card, not state.currentLesson

Added an Edit button to Flashcard/Quiz study screens (reusing the existing card-edit modal) so a confusing or wrong card can be fixed without hunting it down in the lesson's card list. The existing `openEditCard(cardId)` fetched the card by re-querying `state.currentLesson.id`'s full card list and searching for a match — refactored to accept an optional already-loaded card object instead (`openEditCard(cardId, presetCard)`), both to avoid a redundant network round-trip (the study screen already has the full card in `state.studyCards`/`state.quizCards`) and because `state.currentLesson` is the wrong source of truth entirely once a study session spans multiple lessons.

Multi-agent code review caught that this same `state.currentLesson` dependency also broke *saving* the edit: all four card-save handlers built the update request from `state.currentLesson.id`, which is `null` for a session started via "Study Selected" across a multi-lesson selection (the user never opens a single lesson first, so it's never set) — clicking Save in that flow threw `TypeError: Cannot read properties of null (reading 'id')` and silently dropped the edit. Fixed by tracking the card's own `lesson_id` (`state.editingCardLessonId`, set from the card object whenever the edit modal opens) and using that instead, for the update path only — the create-new-card path is unaffected since `openAddCard()` already guards on `state.currentLesson` being set (only reachable from the lesson screen). This same fix incidentally also closed a second, related bug the review found: in local/offline mode, `LocalStorageAdapter.updateCard` looks up the card by `(id, lessonId)` and was silently no-op'ing (found nothing, wrote nothing) whenever the stale `state.currentLesson.id` didn't match the card's real lesson — invisible in server mode (where the SQLite route ignores the lessonId parameter entirely) but a real, silent data-loss bug locally.

**Known limitation, not fixed:** editing a Quiz-mode card *after* it's already been answered in the same session re-renders the fresh question text but keeps the stale pre-edit answer options/correctness highlighting (cached in that card's `quizResults` snapshot, by design — see the quiz-review entry above). The underlying attempt record stays historically accurate; only the review display can look briefly inconsistent until the quiz session restarts. Narrow scenario (requires editing a card you've already answered, in the same run) and self-resolving, so left as-is rather than adding snapshot-invalidation logic.

## 2026-07-22 — Manual difficulty grading (Easy) added as a 3rd button, not a replacement

User feedback: after flipping a card, there was no way to rate *how well* you knew it beyond the binary Still Learning/Know It, and no way to fix a confusing card definition without leaving the study session (see previous entry). Added a 3rd "⚡ Easy" button (key `3`) that sends `grade: "easy"` on the attempt — the server already had a dormant `grade` field (`easy`/`medium`/`hard` → +2/+1/reset SRS steps) that no client had ever populated. Chose to *add* a 3rd button rather than replace the existing 2 or build a 4-tier Again/Hard/Good/Easy row: the ask was explicitly "manual difficulty rating if desired" (optional), and the existing Learning/Know It pair already covers the primary "did I know this" signal — Easy is an opt-in upgrade for "I knew it, and confidently," not a required grading step.

This is orthogonal to the recognition-vs-recall SRS cap (previous entries): `markCard()` always sends `source: "flashcard"`, and the cap's ceiling clamp only triggers on `source === "quiz"`, so Easy-graded flashcard answers are never capped, regardless of current `srs_step`.

Adding a 3rd mark button broke the Flashcard nav row at narrow viewports (Prev/3 mark buttons/Next no longer fit on one line, overflowing off-screen past ~390px). Fixed with a mobile-only layout change: the mark-button group wraps onto its own full-width row (`flex-basis: 100%; order: -1`) with Prev/Next below, rather than shrinking button text/padding further (already near the practical minimum from the pre-existing 600px breakpoint).

## 2026-07-22 — Removed Flashcard toolbar's Reset and "Hard Only" buttons

Direct user feedback: neither button was ever used. Removed entirely — click handlers, `R`/`F` keyboard shortcuts, keymap-modal rows, hint-injection entries, and now-orphaned i18n keys (`common.reset`, `study.hardOnly`, `keymap.resetToStart`, `keymap.filterHardNew`). Incidentally, the removed "Hard Only" handler was reading card difficulty from `localStorage["fc-attempts"]` — the same stale-in-server-mode bug already found and fixed for the on-card difficulty badge (see below) — so deleting the feature also deleted a second instance of that bug for free rather than needing a separate fix.

## 2026-07-22 — Flashcard dot strip reuses known/learning colors for historical difficulty; found and fixed a stale localStorage read

User feedback: every dot in the Flashcard progress strip looked identical (all green) even for a session that hadn't marked any cards yet, because the strip only encoded the current session's `known`/`unknown` boolean — a card with zero session marks got no color class at all, but visually that's indistinguishable from "just happens to render with the base border color," giving no preview of which upcoming cards are actually hard.

Fixed by falling back to the card's *historical* difficulty level (`state.studyStatsMap[card.id].level` — the same `easy`/`medium`/`hard`/`new` computed for the on-card badge, Stats screen, and weighted shuffle) whenever a card hasn't been marked yet this session. Reused the existing `.known` (green) / `.learning` (red) classes for `easy`/`hard` respectively, adding exactly one new class (`.medium`, amber) rather than inventing a parallel color system — a session-fresh "Know It" and a historically-easy card carry the same practical meaning ("you've got this"), so collapsing them onto one color was a deliberate simplification, not an oversight.

While seeding test data to verify the four levels rendered distinctly, found a real, separate bug: the on-card difficulty badge (`fc-diff-badge`) was reading `JSON.parse(localStorage.getItem("fc-attempts") || "[]")` — the `LocalStorageAdapter`'s attempt log, which is never written in server mode — so the badge showed "New" for every card regardless of actual attempt history, contradicting the (correctly-colored) dot right next to it. Fixed by pointing it at the same `state.studyStatsMap` the dots and every other difficulty display already use. This bug predates this session's changes; it was surfaced by the dots fix making the inconsistency visible for the first time, not introduced by it.

## 2026-07-22 — "Needs Recall" filter: client-side srs_step check, auto-switch (not lock) to Flashcard mode

Following the recognition-vs-recall SRS cap (previous entry), users had no way to discover *which* cards were capped except reactively, by answering them in Quiz mode and seeing the hint. Added a "Needs Recall" filter to Study Setup (`srs_step >= RECOGNITION_CAP_STEP AND due`) so capped cards can be proactively batched into a Flashcard session. Initially built without the due-status requirement (any capped card, regardless of schedule), but revised same-day: intersecting with `due` keeps the filter's result set aligned with "things actually worth reviewing right now" rather than surfacing capped-but-not-due cards the user has no immediate reason to touch — consistent with the rest of the app's due-driven review workflow (dashboard due counts, "Review N due" button). This needed no server changes — `srs_step` and `srs_due_at` were already returned on every card object by the existing `/cards/by-lessons` endpoint, so the filter is a pure client-side predicate in `startStudy()`, reusing the same due-check expression as the existing `due` filter.

`RECOGNITION_CAP_STEP=2` is duplicated client-side (with a comment pointing at the server's copy in `server/routes/attempts.js`) rather than fetched from an API — consistent with this codebase's existing tolerance for small hardcoded duplication over adding a config-fetch round-trip for a single tunable integer. Accepted as a known drift risk, same tradeoff already made for the client-side SRS interval display.

A same-day code review flagged that selecting this filter didn't push the user toward Flashcard mode, so someone could pick "Needs Recall" + Quiz and get a session that structurally can't achieve anything (Quiz-correct on an already-capped card just re-plateaus). Chose an **auto-switch, not a lock**: selecting the filter flips the Mode pill to Flashcard automatically (and updates its hint text), but the user can still click back to Quiz manually if they have a reason to. A hard lock (disabling the Quiz pill while this filter is active) was considered and rejected as over-controlling for what's fundamentally a helpful default, not a hard constraint — consistent with the rest of the Setup screen's filter/mode pills being independent, user-overridable choices.

**Known gap, not fixed:** this filter is silently non-functional in local/offline mode (`IS_SERVER===false`) since `LocalStorageAdapter` never tracks `srs_step` at all — it falls back to the existing empty-result alert. This mirrors the pre-existing `due` filter's identical gap in local mode; not a new regression, and local mode is not the primary supported path.

## 2026-07-22 — SRS recognition-vs-recall gate: soft plateau on the resulting step, not the current one

Users can answer a card correctly in Quiz/MCQ mode by *recognizing* the right option, without being able to *recall* the same fact unprompted — a well-documented gap between recognition and free-recall memory (testing effect: Roediger & Karpicke 2006; generation effect: Slamecka & Graf 1978; transfer-appropriate processing: Morris, Bransford & Franks 1977). The app's SRS scheduler previously treated any correct answer identically regardless of which mode produced it, so a card could earn week-long review intervals purely from lucky/pattern-matched MCQ guesses.

Reintroducing a typed "Recall mode" was explicitly rejected — this app had exactly that before (free-text + 3-button self-grade) and removed it 2026-07-02 because "recall added friction that interrupted study flow"; the user independently confirmed the same experience (typing felt slow, self-grading felt subjective). Instead, the fix reuses the two modes that already exist: `server/routes/attempts.js` now reads the `source` field it always stored but never acted on, and clamps SRS step advancement for `source==="quiz"` at `RECOGNITION_CAP_STEP=2` (the 4h/same-day boundary) — only a `source==="flashcard"` correct answer (mentally recall, then flip and self-grade — much closer to true recall) can push a card into 1-day+ intervals.

Chose a **soft plateau over a hard block**: a capped card just keeps coming due at its current short interval until answered correctly via Flashcard, which doesn't trap users who only ever use the app's two quick-quiz shortcuts (`btn-review-due` handler, `openDueReview()`) — both already route through the same central attempts endpoint, so the gate applies uniformly regardless of entry point.

The clamp is computed against the **resulting** step, not just the current one (`if (newStep > ceiling) { newStep = ceiling; ... }` rather than gating only on `curStep >= RECOGNITION_CAP_STEP`) — a multi-agent code review (5 parallel finder agents) caught that gating on `curStep` alone would let a below-cap card leapfrog past the boundary in one hop via the dormant `grade` field (e.g. `curStep=1` + a hypothetical `grade:"easy"` request would jump straight to step 3, uncapped, since `1 >= 2` is false). No shipped client sends `grade` today, but the endpoint accepted it unvalidated from any request body — closing this was a "validate at system boundaries" fix, not just a UI-consistency one. The same review also caught that the per-question inline hint could be silently lost if the *last* question of a quiz session was the capped one and its network response arrived after the results screen had already replaced the quiz screen; fixed by adding an aggregate "N card(s) plateaued this session" note to the Results screen, computed from the session's `quizResults` array rather than depending on that one response's timing.

`RECOGNITION_CAP_STEP=2` is a starting guess, not measured — same-day intervals (10min/1h/4h) stay freely quiz-advanceable since same-day repetition is fine via any method; the 1-day+ transition is where the recognition/recall gap starts to matter for durable retention. It's a single named constant, cheap to retune later.

**Explicitly deferred, not built now:** (1) the difficulty badge (Easy/Medium/Hard) algorithm stays mode-agnostic — conflating it with this same distinction is a separate design question (e.g. does a capped-but-correct quiz answer still count as "easy"?); (2) no per-format weighting — a True/False quiz (50% guess floor) is capped identically to a 4-option MCQ (25% guess floor), which is a real simplification but not worth a second tunable for a v1; (3) no dashboard/SRS-distribution surfacing of *why* a card is plateaued — a user who only checks the dashboard has no in-app explanation for a lesson that keeps coming due at a fixed short cadence, beyond the quiz-screen hint at the moment of the actual plateaued answer.

## 2026-07-21 — Quiz review keyed by card id, not array position; timer race fixed by centralizing the cancel in renderQuizCard()

Quiz review needed a way to know "has this card already been answered" that survives mid-session card deletion (positions shift when a card is spliced out). Rejected a position-indexed `answered[]` array (would need manual re-indexing on every delete — exactly the bug class this was trying to avoid) in favor of a linear scan of `state.quizResults` by `card.id`, which stays correct regardless of array shuffling. A multi-agent code review (5 parallel finder agents, 3 independently) caught a real consequence of adding backward navigation to a session that was built assuming a single forward-only cursor: `answerQuiz()`'s 1200ms auto-advance `setTimeout` was never cancelled by the new Prev/Next-review buttons, so answering a question and then quickly clicking Prev let the stale timer fire later and silently jump the screen forward again, out from under the user. Fixed by storing the timer id on `state.quizAdvanceTimer` and clearing it unconditionally at the top of `renderQuizCard()` — since every navigation path (auto-advance, Prev, review-Next, delete) funnels through that one function, a single cancel point covers all of them rather than adding a `clearTimeout` call to each new handler individually. The same review also caught the delete-card handler trusting `state.quizIndex` at network-response time instead of relocating the card by reference — hardened to use `state.quizCards.indexOf(card)` so a concurrent navigation during the async delete can't cause the wrong card to be spliced out.

## 2026-07-21 — Hand-rolled i18n instead of an external library

The app is an un-bundled vanilla-JS SPA (`client/app.js` served directly, no webpack/vite/rollup step), so any library expecting ES modules or a build pipeline (i18next, FormatJS) would have required adding a bundler just to support translation — a large infrastructure change for a two-language app. Built a minimal `TRANSLATIONS`/`t()`/`applyI18n()` system instead: a plain object dictionary, a lookup function with English fallback, and a DOM-walker driven by `data-i18n*` attributes. The one nonstandard piece — preserving inline SVG icons by only touching a button's last text node instead of overwriting `innerHTML` — avoided a much larger refactor (wrapping every icon+label button's text in a `<span>` across ~40 files' worth of markup). Tradeoff accepted: no pluralization engine, no ICU message format — `{n} card(s)` string literals stand in for real plural rules, acceptable for an EN/VI two-language app where neither language's plural rules are complex enough to need it.

## 2026-07-21 — Icon unification: content emoji and typographic glyphs excluded from the SVG pass

Converting every emoji UI icon to the minimalist SVG set required drawing a line between "app chrome icon" (convert) and things that only look like icons. Excluded, on purpose: (1) `CLASS_ICONS` and any rendered `cls.icon` — these are the user's own personalization choice for a class, not app UI, and swapping them for monochrome line icons would remove a real feature; (2) literal keyboard-key glyphs inside `<kbd>` in the shortcuts modal (`⌫`, arrow keys) — these represent a physical key the user presses, and an abstract "back arrow" icon would misrepresent that; (3) typographic in-sentence arrows like "Term → Definition" (format-picker pill label) and "Image↔Def" (format badge) — these read as text punctuation inside a label, not a standalone actionable icon, and forcing an inline SVG mid-label breaks the text flow without adding clarity. Also left as Unicode: the inline "✓ True"/"✓ " + answer markers inside rendered card-preview text in the lesson card list — these are part of displayed card *data* (showing the correct answer), not interactive UI chrome, same category as the exclusions above.

## 2026-07-20 — Home "Select all" reads from the rendered class list, not `state.homeClasses`

`select-all-classes` used to build `state.selectedClassIds` from `state.homeClasses` — the full, unfiltered list fetched from the server (needed so the archived toggle has data to switch into, see the entry below). Individual class checkboxes only exist for cards actually rendered in `#class-list`, which respects the archived/active view filter — so an archived class hidden in the default view had no checkbox to toggle, yet "Select all" silently added its id anyway. That id then flowed straight into the bulk "Study" → "Due only" flow, which resurfaces due cards for any explicitly-selected lesson regardless of archived status (manual study is intentionally still allowed, see below) — so an archived class's due cards could appear in a session the user never asked for. Fixed by deriving both the "select all" id list and its own checked/total count from `document.querySelectorAll("#class-list [data-class-id]")` (what's actually on screen) instead of the raw fetched array.

## 2026-07-19 — Archived classes: `GET /api/classes` still returns them; exclusion happens per-query in dashboard/stats SQL

Two decisions bundled into the archive feature. (1) Manual study of an archived class is still allowed — archiving only removes it from cross-class "due for review" surfaces (dashboard summary, due-for-review list, streak-adjacent struggling-lesson detection, analytics), not from being opened and studied on demand; confirmed with the user rather than assumed, since "don't review it anymore" could plausibly have meant a hard block. (2) `GET /api/classes` deliberately keeps returning archived rows (with `due_count` forced to 0 via a `CASE` expression) rather than filtering them out server-side — the client's "🗄️ Archived" toggle needs the full list to switch views without a second endpoint or query param. The actual due/summary exclusion happens with `AND c.archived = 0` added to each individual query in `stats.js`'s dashboard and analytics endpoints, because those return pre-aggregated JSON (`dueForReview`, `dueByClass`, `summary`) with no per-class `archived` flag for the client to filter by after the fact — filtering has to happen before aggregation, in SQL. Alternative considered: add an `?archived=0` query param to `GET /api/classes` and have the client request twice (once for active, once for archived) — rejected as an extra round trip for no benefit, since the full list is already small (one query, class-list-sized, not lesson- or card-sized).

## 2026-07-18 — Lesson list clears `innerHTML` after its data promise resolves, not before

Deleting or editing a lesson made the class screen jump to the top of the page, forcing a scroll back down to resume browsing — cards didn't have this problem. Root cause: `_renderLessonItems()` cleared `#lesson-list` synchronously, then awaited `store.getDueLessons(...)` before repopulating — a real gap in which the browser paints an empty list, the document shrinks, and `window.scrollY` gets clamped to fit (there's no explicit `scrollTo` anywhere in this path; the browser does this on its own when content shrinks below the current scroll offset). Once the list refills, nothing restores the old scroll position. `renderCards()` never had this bug because it clears and repopulates in one synchronous block after its single await — no empty-paint gap ever exists. Fix: moved the `list.innerHTML = ""` in `_renderLessonItems()` to right before the repopulation loop, inside the same `.then()` callback, matching `renderCards()`'s pattern — old items stay on screen until the new ones are ready, swapped atomically. Alternative: manually save/restore `window.scrollY` around the re-render — rejected as a workaround for the real defect (the empty-paint gap) rather than removing it; the atomic-swap fix generalizes to every future caller of `_renderLessonItems` for free.

## 2026-07-18 — Overflow moved from `.fc-front`/`.fc-back` to `.fc-content` to fix WebKit backface-visibility bleed-through

The flashcard back face was showing two speaker icons: the back's own plus the front's, mirrored, bleeding through. Root cause: `.fc-front`/`.fc-back` had both `backface-visibility: hidden` and `overflow: auto` inside a `preserve-3d` flip container — a known WebKit bug where `overflow` other than `visible` on a backface-hidden element breaks the hiding, so the face rotated away still renders. Fix: `overflow` moved onto `.fc-content` (the only in-flow child; the audio button/badge/hint are all `position: absolute` and unaffected) with `max-height: 100%; overflow-y: auto; overflow-x: hidden`, leaving `.fc-front`/`.fc-back` with no `overflow` declaration so `backface-visibility` works correctly. Alternative: keep `overflow: auto` on the face and instead try to suppress the bleed with `z-index`/opacity tricks — rejected because it treats the symptom, not the documented cause, and would likely resurface on other WebKit versions.

## 2026-07-16 — Level slicer uses level integer as filter key, not level label

The slicer pill stores the raw level integer (e.g. `"1"`) as `data-filter` and compares it against `String(cls.level)`. Using the label `"L1"` as the key was considered but rejected: the label is presentational and may be formatted differently in the future; comparing raw values avoids a label-to-value mapping step and survives level values that aren't simple integers.

## 2026-07-16 — `[data-class-id]` selector for multi-select instead of `.class-card`

`setHomeSelectMode` and the keyboard arrow-key handler use `#class-list [data-class-id]` rather than `#class-list .class-card`. Both grid cards (`.class-card`) and list rows (`.class-list-row`) carry `data-class-id`, so the attribute selector works for both views without needing to know the current view. Alternative: run two separate queries (`querySelectorAll('.class-card, .class-list-row')`) — rejected for redundancy; the shared attribute is cleaner.

## 2026-07-16 — Dashboard period change re-fetches only analytics, not full dashboard

Clicking a period pill calls `store.getAnalytics(days)` (just heatmap + weekly trend + lesson breakdown) rather than re-running the full `renderDashboard()` which also fetches streak, summary counts, SRS distribution, etc. Those aggregates are not time-windowed (the lesson breakdown is intentionally all-time) so re-fetching them on every period change would be wasteful. The heatmap and trend are the only charts that depend on the window.

## 2026-07-12 — Flashcard swipe: translate `#fc-scene`, not `#fc-card`

The swipe drag transform (`translateX + rotate`) is applied to `#fc-scene` (the wrapper) rather than `#fc-card` (which carries `transform-style: preserve-3d` and the flip `rotateY(180deg)`). Applying translateX directly to `.fc-card` would compose with the flip transform, requiring separate tracking of the current flip state to avoid clobbering `rotateY`. By moving the parent scene instead, the 3D flip mechanism inside the card is untouched regardless of drag position. Alternative: keep both transforms on `.fc-card` using a matrix composition — rejected for complexity and the risk of flip-state drift on snap-back.

## 2026-07-12 — Swipe hints are DOM nodes injected into `#fc-scene`, not pseudo-elements

`✓ Biết rồi` and `✗ Học lại` label nodes are appended to `#fc-scene` at init time (not created per-swipe). They are always in the DOM with `opacity: 0` and `pointer-events: none`. This avoids repeated DOM creation/removal during gesture and keeps `opacity` directly settable from JS (pseudo-elements cannot be animated from JS without CSS custom properties). Alternative: CSS `::before`/`::after` on `.fc-scene` with a `--hint-opacity` var — rejected because two separate custom properties (one per side) plus a `data-dragging-dir` attribute still requires JS involvement; DOM nodes are simpler and no less performant.

## 2026-07-07 — Inline nav buttons use CSS media query + `.hidden` class layering

Dashboard and Analytics in the home header use two overlapping hide mechanisms: the `hidden` class (removed after login via `initUserNav()`) and a `@media (min-width:680px)` CSS rule that switches `.nav-inline-btn` from `display:none` to `display:inline-flex`. This means the buttons start hidden (no flash of content before login), and the responsive behavior is pure CSS with no JS resize listener. On narrow screens the same buttons in the ⋮ dropdown (`.dash-analytics-dropdown`) are suppressed by `display:none !important` from the same media query, so the items move between inline and dropdown without JS. Alternative: toggle classes in a JS `resize` handler — rejected because CSS media queries are simpler, instant, and avoid layout-query overhead.

## 2026-07-07 — Bulk cards: knownMap derived from returned cards instead of separate getKnownMap calls

`getKnownMap(lessonId)` hit `GET /api/lessons/:lessonId/states` which returns only `{card_id: known}` — the new `POST /api/cards/by-lessons` endpoint joins `card_states` and includes `known` in each card row directly. The client derives `knownMap` by iterating the returned cards (`c.known === 1`). This eliminates all N `getKnownMap` calls with no additional server-side cost. Alternative: keep a separate bulk-states endpoint and call it in parallel with bulk-cards — rejected because it added a second request with no benefit since both queries hit the same `card_states` table and the JOIN is the same cost.

## 2026-07-07 — Correlated subquery for last_studied_at replaced with GROUP BY aggregate JOIN

The original `GET /api/lessons/:lessonId/cards` used `(SELECT MAX(created_at) FROM attempts WHERE card_id = cards.id AND user_id = ?) AS last_studied_at` — a correlated subquery that runs once per card. With 100 cards and 3000 attempts, this triggered 100 separate scans of the attempts table. Replaced with `LEFT JOIN (SELECT card_id, MAX(created_at) AS last_studied_at FROM attempts WHERE user_id = ? GROUP BY card_id) la ON la.card_id = cards.id` — a single aggregate pass over attempts, with the result hash-joined once to all cards. The `idx_attempts_cu_created` composite index makes this aggregate nearly free. Applied to both the single-lesson endpoint and the new bulk endpoint.



## 2026-07-03 — Class sort: normalizeLevel helper extracted to prevent POST/PUT divergence

Both POST and PUT `/api/classes` need to coerce the level field (undefined → fallback, null → null, number → Number(val), NaN-string → fallback). The expression was extracted into `normalizeLevel(val, fallback)` in `classes.js` so that future validation changes (e.g., range clamp 1–999, isNaN rejection) are applied in one place. Alternative: inline the expression twice with comments — rejected because it would silently diverge if one site were updated and the other missed.

## 2026-07-03 — Class level: null sorts last (Infinity), not first

Classes with `level=null` are assigned `Infinity` in `sortClasses()` when sorting by level, causing them to sort after all leveled classes, with ties broken by `created_at`. Alternative: sort unlevel'd classes first — rejected because users set levels to promote courses to the top of their list; unlevel'd classes are implicitly "not yet sequenced."

## 2026-07-03 — Preferences localStorage cache updated on save (not just on fetch)

The `btn-save-preferences` handler now writes the new value back to `localStorage` immediately after updating state, before the PUT fetch completes. This ensures that if the user logs out and back in quickly, `loadUserPreferences()` reads the correct cached value synchronously rather than the stale pre-save value. Without this, the stale cache would be applied on the next login until the async fetch resolved.

## 2026-07-02 — Account preferences: PUT uses read-modify-write merge, not full replace

`PUT /api/auth/preferences` reads the current `preferences` JSON column, merges the incoming object over it with `Object.assign`, then writes back. This ensures future preference keys added by new code are not silently deleted when an older client (or a client that only knows about one key) saves. Alternative: send the full preferences object from the client on every save — rejected because the client only knows about the keys it renders in the modal; it cannot preserve server-side keys it is unaware of.

## 2026-07-02 — Recall mode removed

Recall mode (free-recall textarea + three-grade self-assessment) was removed from UI and JS. The DB `source='recall'` value in the `attempts` CHECK constraint was already removed in a prior migration; historical records are preserved. Decision: recall added friction that interrupted study flow.

## 2026-06-28 — True/False: correct stored as lowercase string "true"/"false", displayed as title-case

The T/F answer is stored in the DB as `"true"` or `"false"` (lowercase string) to match the bulk import format (user types `| true`) and the HTML `data-value` attributes. The display layer (`answerQuiz`, `revealRecall`, `renderFlashcard`, `buildQuizOptions`) converts to title-case "True"/"False" at render time. Alternative: store as boolean — rejected because SQLite JSON stores booleans as 0/1, requiring casts in every reader, and the rest of the codebase stores card data as typed strings (MCQ `correct` is a plain string). The conversion is in one place per study mode so maintaining consistency is straightforward.

## 2026-06-27 — Multi-class quiz: home-screen select mode rather than a new screen

The multi-class study flow reuses the same select-mode pattern already used on the class screen (☑ Select button → checkboxes injected in-place → select bar with count and Study button). No intermediate lesson-picker screen was added: selecting classes goes straight to the existing `openSetup()` with all lessons from those classes combined. The existing `setStudyLessonLabel()` pill already labels each card by lesson when `studyScope.lessons.length > 1`, so cross-class sessions are already disambiguated. `returnScreen: "home"` sends the user back to the home screen after the session, which was already handled in the existing return-from-study flow. The `A` key conflict (Analytics vs Select All) is resolved by checking `state.homeSelectMode` first so Select All takes priority in select mode.

## 2026-06-25 — Analytics heatmap: UTC date keys on client match server's SQLite date('unixepoch')

The heatmap generates 90 day-cells on the client and matches them against server-supplied dates from `date(created_at,'unixepoch')` (UTC). Using local-midnight `new Date(y,m,d).toISOString().slice(0,10)` produces the wrong (previous) UTC date for users in UTC+ timezones because `toISOString()` converts local midnight to UTC before formatting. Fixed by using UTC date arithmetic: `new Date(Date.UTC(y,m,d-i))` then `.toISOString().slice(0,10)`, which correctly generates the UTC date the server would return for any given day. Similarly, `getUTCMonth()` and `getUTCDay()` are used for month label and weekday alignment rather than their local-time counterparts.

## 2026-06-25 — Analytics weekly trend uses rolling 7-day buckets, not calendar weeks

The weekly trend chart groups attempts by `(now - created_at) / 604800` integer division — this is a rolling 7-day window anchored to the current second, not Monday–Sunday calendar weeks. The label "This week" therefore means "the past 7 days" rather than the current Mon–Sun week. This was chosen because the server already has Unix timestamps and the formula is simple; calendar-week alignment would require knowing the user's day-of-week locale and aligning to week start. A future improvement could align to ISO weeks, but rolling windows are good enough for a trend chart.

## 2026-06-25 — Audio pronunciation: pointer-events fix + Safari setTimeout workaround

The 3D flip card uses `backface-visibility: hidden` to hide the back face visually, but browsers are not required to block pointer events on hidden backfaces. The back `🔊` button was therefore clickable even when the front was showing. Fixed by adding `.fc-back { pointer-events: none }` and re-enabling only after flip via `.fc-card.flipped .fc-back { pointer-events: auto }`. A separate Safari bug: calling `speechSynthesis.cancel()` then `speak()` synchronously can silently drop the utterance. Fixed by wrapping `speak()` in `setTimeout(fn, 50)`. The 50ms delay is imperceptible to users but gives the browser time to complete the cancel before starting the new utterance.

## 2026-06-27 — TTS voice selection: prefer Google/Enhanced voices, synchronous Safari init

`_pickVoice()` ranks voices: Google en-US → Enhanced/Premium/Neural en-US → any en-US → any English. Safari never fires `voiceschanged`, so the voice is also picked synchronously at script load (when `getVoices()` returns immediately on Safari). The `voiceschanged` listener is guarded with `typeof addEventListener === "function"` to avoid crashing on old Android WebViews that expose `speechSynthesis` without the full `EventTarget` interface. `rate = 0.9` gives slightly more natural cadence than the default 1.0 without noticeably slowing speech.

## 2026-06-27 — Flashcard mobile: -webkit-backface-visibility for Safari face bleed-through

On Safari/iOS, `backface-visibility: hidden` alone sometimes fails to hide the non-facing side of a 3D-transformed card, causing both front and back audio buttons to appear when flipped. Adding `-webkit-backface-visibility: hidden` alongside the unprefixed property forces Safari to apply the correct backface culling.

## 2026-06-26 — Keyboard list navigation: DOM focus (tabIndex=-1) over state index

Arrow-key navigation for class cards and lesson items uses `tabIndex=-1` + `element.focus()` (DOM-native focus management) rather than tracking a `state.focusedIndex` integer. The DOM-native approach is simpler: `moveFocus()` reads `document.activeElement`, computes the next index with `Array.indexOf`, and calls `.focus()`. No state to sync, no stale-index bugs after re-renders. Trade-off: if `renderLessons()` is called while the user is mid-navigation (e.g., after toggling select mode via `setSelectMode`), focus resets to `<body>` because `innerHTML` is wiped. Acceptable for the current UX: `setSelectMode` in-place patches the DOM without a full re-render, so the common keyboard flow (navigate → select → study) doesn't lose focus.

## 2026-06-25 — Keyboard shortcuts: unified handler + Escape-closes-any-modal

Three scattered `keydown` listeners (flashcard, quiz, recall) were replaced with a single consolidated handler at the end of `app.js`. The unified handler dispatches by `getActiveScreen()` (reads the DOM `.screen.active` element — the DOM is the sole source of truth since `showScreen()` never stores the ID in `state`). `isInputFocused()` blocks shortcuts when focus is in INPUT/TEXTAREA/SELECT. `?` and `Escape` are handled before the input-focus guard so they work globally. Escape priority: keymap modal > overlay modal (via `closeAllModals()`) > share modal > prompt-guide modal > per-screen navigation. This ensures Escape always does "the most local" thing rather than accidentally navigating away while a modal is open.

## 2026-06-26 — Image upload: two-step save (upload-then-create)

Image cards use a two-step flow: upload the image first (`POST /api/upload` → returns `{url}`), then save the card with the URL in `data.imageUrl`. The alternative — multipart form submission combining image + JSON in one request — was rejected because it would require parsing `data` fields out of a multipart body alongside the file, mixing form-data encoding with JSON. The two-step approach keeps card creation as a clean JSON POST that matches all other card formats, and the upload endpoint remains a focused single-purpose route. Trade-off: an upload can succeed but the subsequent card creation can fail, leaving an orphaned file. Accepted as low-risk for a single-user or small-team context.

## 2026-06-26 — Upload race condition fix: sequence counter

`handleImageFile()` sets a module-level `uploadSeq` counter and captures `mySeq = ++uploadSeq` at call time. The fetch `.then()` callback only writes to `stagedImageUrl` if `uploadSeq === mySeq` (i.e., no newer upload started). This is simpler than `AbortController` (which would require cancelling in-flight requests) and sufficient because only the last-picked image should be staged — stale completions are simply ignored.

## 2026-06-26 — Schema migrations: `schema_migrations` table for idempotency

The previous table-copy migration pattern (`CREATE TABLE IF NOT EXISTS _v2 → INSERT OR IGNORE → DROP old → RENAME`) re-ran every server startup because the `_v2` table was renamed away each time. Added a `schema_migrations` table with `name TEXT PRIMARY KEY`; each migration inserts its name before the destructive DROP so subsequent startups skip it. The insert happens before DROP so a crash between insert and DROP leaves the migration marked as done but the old table intact — safe because the old table still has the correct data and the migration's purpose (removing a CHECK constraint) is a no-op if the constraint is already gone in practice.

## 2026-06-18 — SQLite via node-sqlite3-wasm
Use WASM-based SQLite (no native build) for Railway compatibility. Downside: async init. Lock file `flashcards.db.lock` removed on startup to survive container restarts.

## 2026-06-23 — Sessions migrated from MemoryStore to SQLiteSessionStore
MemoryStore is cleared on every Railway container restart, forcing re-login on mobile rotation (which triggers a reload). Replaced with a custom `SQLiteSessionStore` (server/sessionStore.js) that writes to the existing SQLite DB using a `sessions` table. The store implements only `get/set/destroy/touch` — the minimum express-session contract. A 24-hour `setInterval` sweeps expired rows. No external dependency needed since node-sqlite3-wasm is already bundled.

## 2026-06-23 — Screen state persisted via localStorage key fc-last-screen
The SPA had no URL routing — page refresh always landed on home. Added `saveScreenState()` called at navigation points (openClass, openLesson, back buttons) and `restoreLastScreen()` called on init/login/register. Only home/class/lesson are persisted; transient screens (study, quiz, flashcard) are not. Stale entries for deleted entities fall back to home via the `.catch()` path. The key is cleared on logout and on fresh registration to prevent cross-user state bleed.

## 2026-06-18 — Sessions via express-session + MemoryStore (superseded 2026-06-23)
Simple session approach for single-instance Railway deployment. Replaced by SQLiteSessionStore.

## 2026-06-18 — Google OAuth without passport
Implemented raw OAuth2 code-exchange to avoid passport setup complexity. fetch() to Google token and userinfo endpoints directly.

## 2026-06-19 — Per-card SRS step-based intervals (replaces session-level; superseded 2026-08-06 by FSRS, see top of this file)
Replaced the session-level score-based scheduler with per-card spaced repetition. Each card has its own `srs_step` (0–6) and `srs_due_at` (unix seconds) stored in `card_states`. Correct answer advances the step; wrong resets to 0. Steps map to intervals: 10min → 1h → 4h → 1d → 3d → 7d → 21d. This means a card the user just learned will resurface in 10 minutes, then again in an hour — much faster feedback than the old session-level 4h minimum. Cards are "due" when `srs_due_at <= now`. Due counts surface on class cards, lesson list, and dashboard (grouped by class). Decision: step-based rather than SM-2/FSRS to keep the implementation simple while delivering the core SRS benefit; FSRS remains a future upgrade path.

## 2026-06-18 — Spaced repetition intervals (superseded)
Simple fixed intervals based on quiz score: ≥90% → 7d, ≥70% → 3d, ≥50% → 1d, <50% → 4h. Stored in quiz_sessions table. Replaced 2026-06-19 by per-card step-based intervals.

## 2026-06-18 — Progressive difficulty via weighted shuffle
Hard cards (blended error ≥0.6) appear 3× in weighted pool, medium (0.3–0.6) 2×, easy/new 1×. Deduplication after shuffle preserves front-loading effect.

## 2026-06-18 — Interleaved study as default for multi-lesson sessions
When 2+ lessons are selected, cards from all lessons are pooled and shuffled together (interleaved) by default rather than completing each lesson in sequence (blocked). Backed by learning science: a 2024 *Learning and Instruction* study confirmed interleaved practice produces more durable retention than blocked practice. Users can switch to Blocked via a "Card Order" pill on the setup screen. The pill is hidden for single-lesson sessions where the distinction is moot.

## 2026-06-18 — Confidence-Based Repetition (CBR) deferred
CBR (1–5 post-answer confidence rating modulates next review interval) was researched and found to have solid evidence for improving long-term recall. Decision: deferred. The per-card rating UI felt too intensive in user testing — it interrupts the study flow on every card. Will revisit if users request finer-grained spaced repetition control.

## 2026-06-18 — FSRS algorithm skipped for now
FSRS (Free Spaced Repetition Scheduler) achieves 20–30% fewer reviews than SM-2 for the same 90% retention target. An open-source JS library is available. Decision: skipped for now. The existing fixed-interval scheduler (score-based: ≥90% → 7d, ≥70% → 3d, etc.) is good enough for the current user base. FSRS can be added as a future enhancement without changing the quiz flow — only the interval-calculation step changes.

## 2026-06-23 — Card checkbox change event + item click guard for select mode
In card select mode, a guard `if (e.target.tagName === "INPUT") return` on the item's click listener prevents double-toggling when the user clicks the checkbox (the click would bubble to the item, firing toggleCardSelection twice). However, this alone breaks direct checkbox clicks — the native checkbox state changes but `state.selectedCardIds` doesn't. Fixed by also attaching a `change` event on each card's checkbox that calls `toggleCardSelection`. Result: clicking the item body fires one toggle (via item click); clicking the checkbox fires one toggle (via change); the bubble from checkbox click is suppressed by the guard.

## 2026-06-18 — Modal architecture fix: scope closeAllModals to overlay container
`closeAllModals()` was calling `document.querySelectorAll(".modal")`, which also matched inner divs inside `#modal-share` and `#modal-prompt-guide` (those overlays contain child elements that also carry the `.modal` class). This caused closing any modal to blank-screen those overlays. Fixed by scoping the selector to `"#modal-overlay .modal"` so only the primary overlay's modal content is targeted. Also added an explicit `.modal` classList `.remove("hidden")` guard inside `openShareModal()` and the prompt-guide opener as a defensive reset against stale state.

## 2026-06-18 — Dev reset token hidden in production
`POST /api/auth/forgot-password` was returning `_devResetUrl` in the JSON response unconditionally, allowing anyone to grab a password-reset token without SMTP access. Fixed: the field is now only included when `NODE_ENV !== "production"`. In production without SMTP configured, the endpoint returns `{ok:true}` with no token exposed, and the reset email is silently dropped (no SMTP = no email sent, user experience unchanged).

## 2026-06-19 — Variable MCQ choices: dynamic rows over static 5 fields
MCQ cards now support 1–4 distractors (2–5 total choices) instead of a fixed 3. Two UI approaches were considered: (A) 5 static input fields with the first required and others optional, (B) dynamic rows with add/remove buttons starting from 1. Chose B: MCQ authoring is deliberate, not a fast-input flow, so clean UX (only the fields you need) outweighs the small amount of extra JS. Server validates `distractors.length` is 1–4 on both single and bulk card endpoints. `clearDistractorList()` caps at 4 rows on load to guard against corrupt DB data.

## 2026-07-11 — Search uses command-palette pattern (standalone overlay) over filter-in-place

Two industry patterns were considered for global search: (A) filter-in-place — search bar in the main nav that filters the current list in real time; (B) command palette — modal overlay opened by Ctrl/Cmd+K that searches all content types simultaneously. Chose B because (A) only works on the currently visible screen and can't reach across classes/lessons/cards in one step; the command palette pattern (Linear, Notion, VS Code) is better suited for navigating a hierarchical content tree without knowing exactly where something lives. The standalone modal also avoids adding persistent nav space for a feature most users won't use every interaction.

## 2026-07-11 — Card text extracted server-side via CASE/json_extract rather than full-scan in client

The search endpoint needed to search card text across four formats (term-def, mcq, true-false, image-def), each storing text in a different JSON field. Options: (A) fetch all card JSON rows and parse in JS; (B) use a stored/generated column; (C) use a CASE expression in the WHERE/SELECT with `json_extract`. Chose C: it runs a single SQL pass with no extra storage, no migration, and no application-layer JSON parsing. The CASE expression is duplicated between the SELECT alias and the WHERE COALESCE — a known SQLite limitation (no reference to SELECT aliases in WHERE), but SQLite query planning computes it efficiently.

## 2026-06-18 — Multi-agent development workflow adopted
Established a three-role agent loop: Planner (Plan subagent) produces step-by-step implementation specs; Executor (main Claude session) implements them; Reviewer (general-purpose subagent) audits the diff for correctness and security. A fourth Research agent (general-purpose with WebSearch) is spun up on-demand for evidence gathering. This separation prevents context contamination between planning and implementation and gives each role a clean starting point.
